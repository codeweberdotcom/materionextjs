# ТЕХНИЧЕСКОЕ ЗАДАНИЕ ДЛЯ AI АГЕНТА: МИГРАЦИЯ ПРОЕКТА С NEXTAUTH НА LUCIA AUTH

## 🎯 ОБЩАЯ ИНФОРМАЦИЯ

**Задача:** Полная миграция аутентификации с NextAuth.js на Lucia Auth v3.x
**Проект:** Materio MUI Next.js Admin Template
**Текущая версия:** NextAuth v4.24.11
**Целевая версия:** Lucia Auth v3.x

## 📋 ПРЕДВАРИТЕЛЬНЫЕ УСЛОВИЯ

### Перед началом миграции:
1. **Создать резервную копию** всех файлов аутентификации
2. **Остановить сервер разработки**
3. **Создать тестовую базу данных** для проверки миграции
4. **Убедиться в наличии** всех переменных окружения

### Требуемые переменные окружения:
```env
NEXTAUTH_SECRET=your-secret-key
NEXTAUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
DATABASE_URL=your-database-url
```

## 🔄 ПОШАГОВЫЙ ПЛАН МИГРАЦИИ

### ШАГ 1: УСТАНОВКА ЗАВИСИМОСТЕЙ

**Цель:** Установить Lucia Auth и удалить NextAuth

**Действия:**
```bash
# Установить Lucia
npm install lucia @lucia-auth/adapter-prisma

# Удалить NextAuth зависимости
npm uninstall next-auth @auth/prisma-adapter jsonwebtoken @types/jsonwebtoken
```

**Критерии выполнения:**
- ✅ `lucia` установлен в package.json
- ✅ `@lucia-auth/adapter-prisma` установлен
- ✅ `next-auth` удален из зависимостей
- ✅ `jsonwebtoken` удален из зависимостей

**При ошибке:** Восстановить package.json из резервной копии

---

### ШАГ 2: СОЗДАНИЕ LUCIA КОНФИГУРАЦИИ

**Цель:** Создать базовую конфигурацию Lucia Auth

**Файл:** `src/libs/lucia.ts` (новый файл)

**Код:**
```typescript
import { Lucia } from 'lucia';
import { PrismaAdapter } from '@lucia-auth/adapter-prisma';
import { prisma } from './prisma';

export const lucia = new Lucia(new PrismaAdapter(prisma.session, prisma.user), {
  sessionCookie: {
    expires: false,
    attributes: {
      secure: process.env.NODE_ENV === 'production',
    },
  },
  getUserAttributes: (attributes) => {
    return {
      id: attributes.id,
      email: attributes.email,
      name: attributes.name,
      role: attributes.role,
      permissions: attributes.permissions,
    };
  },
});

declare module 'lucia' {
  interface Register {
    Lucia: typeof lucia;
    DatabaseUserAttributes: {
      id: string;
      email: string;
      name: string;
      role: string;
      permissions: string;
    };
  }
}
```

**Критерии выполнения:**
- ✅ Файл `src/libs/lucia.ts` создан
- ✅ Lucia экземпляр экспортирован
- ✅ Типы определены корректно
- ✅ Prisma адаптер настроен

---

### ШАГ 3: ОБНОВЛЕНИЕ ПРОВЕРКИ ПОЛЬЗОВАТЕЛЕЙ

**Цель:** Заменить `getServerSession` на Lucia методы

**Файлы для изменения:**
- Все API роуты с `getServerSession`
- `middleware.ts`
- HOC компоненты (`AuthGuard.tsx`, `GuestOnlyRoute.tsx`)

**Пример замены:**
```typescript
// Было
import { getServerSession } from 'next-auth'
const session = await getServerSession(authOptions)

// Стало
import { lucia } from '@/libs/lucia'
const session = await lucia.validateSession(sessionId)
```

**Критерии выполнения:**
- ✅ Все импорты `getServerSession` заменены
- ✅ Все импорты `authOptions` удалены
- ✅ API роуты возвращают корректные ответы
- ✅ Middleware работает правильно

---

### ШАГ 4: СОЗДАНИЕ API РОУТОВ LUCIA

**Цель:** Создать новые API роуты для Lucia

**Файл:** `src/app/api/auth/[...lucia]/route.ts` (новый)

**Код:**
```typescript
import { lucia } from '@/libs/lucia';

export const GET = lucia.handleRequest;
export const POST = lucia.handleRequest;
```

**Дополнительные роуты:**
- `src/app/api/auth/login/route.ts` - вход
- `src/app/api/auth/logout/route.ts` - выход
- `src/app/api/auth/google/route.ts` - Google OAuth

**Критерии выполнения:**
- ✅ API роуты Lucia созданы
- ✅ Вход/выход работает
- ✅ Google OAuth функционирует

---

### ШАГ 5: ОБНОВЛЕНИЕ КЛИЕНТСКОГО КОДА

**Цель:** Заменить NextAuth хуки на Lucia

**Файлы для изменения:**
- `src/contexts/nextAuthProvider.tsx` → `src/contexts/authProvider.tsx`
- Все компоненты с `useSession`

**Пример замены:**
```typescript
// Было
import { useSession } from 'next-auth/react'
const { data: session } = useSession()

// Стало
import { useAuth } from '@/hooks/useAuth'
const { user, session } = useAuth()
```

**Критерии выполнения:**
- ✅ Провайдер заменен
- ✅ Все `useSession` заменены
- ✅ UI компоненты работают корректно

---

### ШАГ 6: ОБНОВЛЕНИЕ БАЗЫ ДАННЫХ

**Цель:** Обновить схему Prisma для Lucia

**Файл:** `prisma/schema.prisma`

**Изменения:**
```prisma
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  role      String   @default("user")
  permissions String @default("[]")

  // Lucia поля
  authId    String?  @unique

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Session {
  id        String   @id @default(cuid())
  userId    String
  expiresAt DateTime

  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
```

**Критерии выполнения:**
- ✅ Схема обновлена
- ✅ Миграция создана
- ✅ База данных обновлена

---

### ШАГ 7: ТЕСТИРОВАНИЕ

**Цель:** Полное тестирование функциональности

**Тест-кейсы:**
1. ✅ Вход по email/password
2. ✅ Выход из системы
3. ✅ Google OAuth
4. ✅ Защита API роутов
5. ✅ Ролевая система
6. ✅ Middleware
7. ✅ UI компоненты

**Критерии выполнения:**
- ✅ Все тесты проходят
- ✅ Нет ошибок в логах
- ✅ Производительность не ухудшилась

---

### ШАГ 8: ОЧИСТКА И ДОКУМЕНТАЦИЯ

**Цель:** Удалить старый код и создать документацию

**Действия:**
1. Удалить все файлы NextAuth
2. Обновить переменные окружения
3. Создать документацию миграции
4. Обновить README

**Критерии выполнения:**
- ✅ NextAuth код полностью удален
- ✅ Переменные окружения обновлены
- ✅ Документация создана

## 🚨 ОБРАБОТКА ОШИБОК

### При ошибке на любом шаге:
1. **Остановить выполнение**
2. **Восстановить из резервной копии**
3. **Проанализировать причину ошибки**
4. **Исправить проблему**
5. **Возобновить с проблемного шага**

### Критические ошибки:
- **База данных повреждена** → Восстановить из бэкапа
- **API роуты не работают** → Проверить конфигурацию Lucia
- **Аутентификация сломана** → Проверить переменные окружения

## ✅ КРИТЕРИИ УСПЕХА

### Функциональные:
- [ ] Пользователи могут входить/выходить
- [ ] API роуты защищены корректно
- [ ] Ролевая система работает
- [ ] Google OAuth функционирует
- [ ] UI обновляется при изменении сессии

### Технические:
- [ ] Нет ошибок в логах
- [ ] Все тесты проходят
- [ ] Bundle size уменьшился
- [ ] Производительность улучшилась

### Качественные:
- [ ] Код проще поддерживать
- [ ] Лучшая масштабируемость
- [ ] Современные практики безопасности
- [ ] Готова основа для будущих фич

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

- **Уменьшение зависимостей:** -4 пакета
- **Улучшение производительности:** +15-20%
- **Упрощение кода:** -30% строк кода
- **Лучшая безопасность:** Современные алгоритмы
- **Масштабируемость:** Готова к распределенным системам

---

**Время выполнения:** 5-7 дней
**Ресурсы:** 1 разработчик
**Риски:** Низкие (пошаговая миграция)
**Откат:** Возможен на любом этапе