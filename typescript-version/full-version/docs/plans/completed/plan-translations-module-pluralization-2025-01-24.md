# План: Множественные формы (Pluralization) для модуля переводов

**Статус:** ✅ Завершён  
**Дата завершения:** 2025-01-24  
**Модуль:** Переводы (Translations)

---

## 📋 Обзор

Реализация поддержки множественных форм (pluralization) для языков с комплексными правилами склонения (русский, арабский, польский и др.) в модуле переводов.

---

## ✅ Выполненные задачи

### 1. Создание утилит для pluralization

**Файл:** `src/utils/translations/pluralization.ts`

- ✅ Реализованы правила множественных форм для русского языка:
  - `one`: 1, 21, 31, 41...
  - `few`: 2-4, 22-24, 32-34...
  - `many`: 0, 5-20, 25-30...
- ✅ Реализованы правила для английского, французского и арабского языков
- ✅ Функция `getPluralForm()` для определения формы по числу и языку
- ✅ Функция `formatTranslation()` для форматирования с подстановкой переменных
- ✅ Поддержка форматов `{{var}}` и `${var}` для переменных

### 2. Создание хука useTranslate

**Файл:** `src/hooks/useTranslate.ts`

- ✅ Создан хук `useTranslate()` с поддержкой plural forms
- ✅ Автоматическое определение формы множественного числа на основе языка
- ✅ Поддержка переменных в переводах
- ✅ Обработка как JSON-строк из БД, так и объектов из JSON файлов
- ✅ Поддержка вложенных ключей через точку

### 3. Обновление валидации

**Файл:** `src/lib/validations/translation-schemas.ts`

- ✅ Обновлена схема валидации для поддержки plural forms
- ✅ Поддержка значения как строки, так и JSON-объекта с формами

### 4. Обновление импорта/экспорта

**Файл:** `src/utils/translations/export-helper.ts`

- ✅ Обновлена функция `processTranslationValue()` для обработки объектов plural forms
- ✅ При импорте: объекты из JSON преобразуются в JSON-строки для БД
- ✅ При экспорте: JSON-строки из БД преобразуются обратно в объекты

### 5. Добавление plural forms в русский словарь

**Файл:** `src/data/dictionaries/ru.json`

- ✅ Добавлены plural forms для ключей с числовыми переменными:
  - `daysAgo`, `hoursAgo`, `minutesAgo`
  - `totalUsers`, `totalUsersCount`
  - `confirmBulkActivate`, `confirmBulkDeactivate`, `confirmBulkDelete`
  - `bulkOperationSuccess`, `bulkOperationPartialSuccess`
  - `errorsCount`, `warningsCount`, `duplicatesFound`
  - `recordsExported`, `recordsImported`, `recordsFailed`, `recordsProcessed`
  - `rowsWillBeSkipped`, `translationsImportSuccess`
  - `waitMessage`, `rateLimitMessage`, `rateLimitWarning`
  - `viewingLabel`, `tableSubtitle`

### 6. Обновление UI компонентов

**Файл:** `src/views/apps/references/translations/AddTranslationDialog.tsx`

- ✅ Добавлена возможность ввода plural forms через UI
- ✅ Переключатель между обычным значением и plural forms
- ✅ Поля для ввода `one`, `few`, `many`, `other`

**Файл:** `src/views/apps/references/translations/TranslationsListTable.tsx`

- ✅ Добавлены фильтры для колонок таблицы
- ✅ Добавлены спиннеры для кнопок импорта/экспорта
- ✅ Исправлены переводы для состояний загрузки

### 7. Исправление ошибок использования

**Файл:** `src/views/apps/roles/RoleCards.tsx`

- ✅ Исправлена ошибка `TypeError: t.navigation.totalUsersCount.replace is not a function`
- ✅ Заменён прямой доступ к словарю на использование `useTranslate()`
- ✅ Корректная обработка plural forms в компоненте

---

## 📝 Изменённые файлы

### Созданные файлы
- `src/utils/translations/pluralization.ts` - Утилиты для pluralization
- `src/hooks/useTranslate.ts` - Хук для переводов с поддержкой plural

### Обновлённые файлы
- `src/lib/validations/translation-schemas.ts` - Валидация plural forms
- `src/utils/translations/export-helper.ts` - Импорт/экспорт plural forms
- `src/views/apps/references/translations/AddTranslationDialog.tsx` - UI для plural forms
- `src/views/apps/references/translations/TranslationsListTable.tsx` - Фильтры и спиннеры
- `src/views/apps/roles/RoleCards.tsx` - Использование useTranslate
- `src/data/dictionaries/ru.json` - Plural forms для русского языка
- `src/data/dictionaries/en.json`, `fr.json`, `ar.json` - Переводы для UI

---

## 🔧 Технические детали

### Хранение данных

- **В БД:** Plural forms хранятся как JSON-строка в поле `value`
  ```json
  "{\"one\":\"{{count}} пользователь\",\"few\":\"{{count}} пользователя\",\"many\":\"{{count}} пользователей\"}"
  ```

- **В JSON файлах:** Plural forms хранятся как объекты
  ```json
  {
    "one": "{{count}} пользователь",
    "few": "{{count}} пользователя",
    "many": "{{count}} пользователей"
  }
  ```

### Использование

```typescript
import { useTranslate } from '@/hooks/useTranslate'

const { t } = useTranslate()

// Простой перевод
t('navigation.dashboard') // → "Панель управления"

// С переменными
t('messages.welcome', { name: 'Иван' }) // → "Привет, Иван!"

// С plural (для русского языка)
t('navigation.totalUsersCount', { count: 1 }) // → "Всего 1 пользователь"
t('navigation.totalUsersCount', { count: 3 }) // → "Всего 3 пользователя"
t('navigation.totalUsersCount', { count: 5 }) // → "Всего 5 пользователей"
```

### Поддерживаемые форматы переменных

- `{{count}}` - стандартный формат
- `${count}` - альтернативный формат (также поддерживается)

---

## 📚 Документация

Документация добавлена в `ROOT_FILES_DESCRIPTION.md`:

- ✅ Описание модуля "Переводы"
- ✅ Раздел "Множественные формы (Pluralization)"
- ✅ Структура файлов
- ✅ Примеры использования

---

## ✅ Соответствие стандартам

Все изменения соответствуют `ROOT_FILES_DESCRIPTION.md`:

- ✅ Стандартизированная обработка ошибок
- ✅ Валидация через Zod схемы
- ✅ События (EventService) для всех операций
- ✅ Метрики Prometheus
- ✅ Роль-базированный доступ (по `role.code`)
- ✅ Автоматический экспорт после изменений

---

## 🎯 Результаты

1. ✅ Полная поддержка plural forms для русского и других языков
2. ✅ Удобный API через хук `useTranslate()`
3. ✅ UI для создания и редактирования plural forms
4. ✅ Корректная работа импорта/экспорта
5. ✅ Все ошибки использования исправлены
6. ✅ Документация обновлена

---

## 📝 Примечания

- Формат переменных `${var}` был добавлен для совместимости с существующими переводами
- Plural forms автоматически преобразуются между форматами (объект ↔ JSON-строка) при импорте/экспорте
- Поддержка других языков (арабский, польский и др.) реализована, но требует добавления соответствующих форм в словари

---

**План завершён:** 2025-01-24


