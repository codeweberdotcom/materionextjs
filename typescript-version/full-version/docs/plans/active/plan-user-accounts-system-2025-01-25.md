# План реализации системы аккаунтов для пользователей

**Дата создания:** 2025-01-25  
**Статус:** Планирование  
**Приоритет:** Высокий

---

## 🎯 Цель

Создать систему аккаунтов, где один пользователь может иметь несколько аккаунтов с разными тарифными планами. Тарифные планы привязаны к аккаунтам, а не к пользователям. Реализовать три типа регистрации с автоматическим созданием базового аккаунта, систему управления сетью компаний с назначением менеджеров и возможность передачи аккаунтов.

---

## 📋 Связанные документы

- [ROOT_FILES_DESCRIPTION.md](../../ROOT_FILES_DESCRIPTION.md)
- [AI_MODULE_STANDARDS](../../AI_MODULE_STANDARDS.md)
- [AI_WORKFLOW_GUIDE](../../AI_WORKFLOW_GUIDE.md)

---

## ⏱️ Сроки

- **Начало:** 2025-01-25
- **Планируемое окончание:** 2025-02-15
- **Фактическое окончание:** (заполняется после завершения)

---

## 📊 Требования

### Функциональные требования

1. **Типы аккаунтов при регистрации:**
   - Для публикации объявления → создается аккаунт с типом `LISTING` и тарифом `FREE`
   - Для размещения компании → создается аккаунт с типом `COMPANY` и тарифом `FREE`
   - Сеть компаний → создается аккаунт с типом `NETWORK` и тарифом `FREE`, открывается возможность создавать дополнительные аккаунты

2. **Множественные аккаунты:**
   - Один пользователь может иметь несколько аккаунтов
   - Каждый аккаунт имеет свой тарифный план
   - Аккаунт не имеет логина и пароля (привязан к пользователю)

3. **Сеть компаний:**
   - Возможность создавать несколько аккаунтов
   - Назначение других пользователей для управления аккаунтами (один пользователь на один аккаунт)
   - Назначенный пользователь должен быть зарегистрирован в системе
   - Основной пользователь может запретить управление
   - Назначенный пользователь видит только один аккаунт, которым ему дали управлять
   - Тарифом управляет основной пользователь

4. **Передача аккаунта:**
   - Возможность передать аккаунт в полное управление назначенному пользователю
   - Требуется согласие назначенного пользователя

5. **Переключение аккаунтов:**
   - Быстрое переключение между аккаунтами через выпадающий список в хедере (MUI стиль)
   - Текущий выбранный аккаунт сохраняется в сессии/контексте

---

## 📊 Этапы реализации

### Этап 1: База данных и модели данных

**Цель:** Создать Prisma модели для аккаунтов, тарифных планов и связей

**Задачи:**

- [ ] 1.1 Создать модель `TariffPlan` (тарифные планы)
- [ ] 1.2 Создать модель `UserAccount` (аккаунты пользователей)
- [ ] 1.3 Создать модель `AccountManager` (назначенные менеджеры)
- [ ] 1.4 Создать модель `AccountTransfer` (запросы на передачу аккаунта)
- [ ] 1.5 Добавить связи между моделями
- [ ] 1.6 Создать индексы для оптимизации запросов
- [ ] 1.7 Выполнить миграцию базы данных

**Структура моделей:**

```prisma
model TariffPlan {
  id          String   @id @default(cuid())
  code        String   @unique // 'FREE', 'BASIC', 'PRO', 'ENTERPRISE'
  name        String   @unique // Отображаемое имя
  description String?
  price       Float    @default(0) // Месячная стоимость
  currency    String   @default("RUB")
  features    String   @default("{}") // JSON с возможностями
  maxAccounts Int?     // Максимум аккаунтов (для NETWORK)
  isActive    Boolean  @default(true)
  isSystem    Boolean  @default(false) // Системный тариф (нельзя удалить)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  accounts    UserAccount[]
  
  @@index([code])
  @@index([isActive])
}

model UserAccount {
  id          String   @id @default(cuid())
  userId      String   // Владелец аккаунта
  ownerId     String   // Основной владелец (для переданных аккаунтов)
  type        String   // 'LISTING', 'COMPANY', 'NETWORK'
  name        String   // Название аккаунта
  description String?
  tariffPlanId String
  tariffPlan  TariffPlan @relation(fields: [tariffPlanId], references: [id])
  user        User     @relation("OwnedAccounts", fields: [userId], references: [id], onDelete: Cascade)
  owner       User     @relation("OwnedAccountsByOwner", fields: [ownerId], references: [id])
  status      String   @default("active") // 'active', 'suspended', 'archived'
  metadata    String?  @default("{}") // JSON: дополнительные данные
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  managers    AccountManager[]
  transfers   AccountTransfer[] @relation("TransferFrom")
  receivedTransfers AccountTransfer[] @relation("TransferTo")
  
  @@index([userId])
  @@index([ownerId])
  @@index([type])
  @@index([status])
  @@index([tariffPlanId])
}

model AccountManager {
  id        String   @id @default(cuid())
  accountId String
  userId    String   // Назначенный пользователь
  account   UserAccount @relation(fields: [accountId], references: [id], onDelete: Cascade)
  user      User     @relation("ManagedAccounts", fields: [userId], references: [id], onDelete: Cascade)
  canManage Boolean  @default(true) // Может ли управлять
  canEdit   Boolean  @default(true) // Может ли редактировать
  canDelete Boolean  @default(false) // Может ли удалять
  assignedAt DateTime @default(now())
  assignedBy String   // Кто назначил
  revokedAt  DateTime?
  revokedBy  String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  
  @@unique([accountId, userId])
  @@index([userId])
  @@index([accountId])
  @@index([canManage])
}

model AccountTransfer {
  id          String   @id @default(cuid())
  fromAccountId String
  toUserId    String   // Кому передается
  fromAccount UserAccount @relation("TransferFrom", fields: [fromAccountId], references: [id], onDelete: Cascade)
  toUser      User     @relation("AccountTransfers", fields: [toUserId], references: [id], onDelete: Cascade)
  status      String   @default("pending") // 'pending', 'accepted', 'rejected', 'cancelled'
  requestedBy String   // Кто запросил передачу
  acceptedAt  DateTime?
  rejectedAt  DateTime?
  cancelledAt DateTime?
  reason      String?  // Причина отклонения/отмены
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([fromAccountId])
  @@index([toUserId])
  @@index([status])
}

// Обновление модели User
model User {
  // ... существующие поля ...
  ownedAccounts      UserAccount[] @relation("OwnedAccounts")
  ownedAccountsByOwner UserAccount[] @relation("OwnedAccountsByOwner")
  managedAccounts    AccountManager[] @relation("ManagedAccounts")
  accountTransfers   AccountTransfer[] @relation("AccountTransfers")
}
```

**Критерии завершения:**

- [ ] Миграция успешно выполнена
- [ ] Все связи корректны
- [ ] Индексы созданы
- [ ] Seed данные для тарифных планов созданы

**Оценка времени:** 1 день

---

### Этап 2: Типы и валидация

**Цель:** Создать TypeScript типы и Zod схемы для валидации

**Задачи:**

- [ ] 2.1 Создать `src/types/accounts/types.ts` с типами:
  - `AccountType` enum ('LISTING', 'COMPANY', 'NETWORK')
  - `AccountStatus` enum ('active', 'suspended', 'archived')
  - `TransferStatus` enum ('pending', 'accepted', 'rejected', 'cancelled')
  - `TariffPlanCode` enum ('FREE', 'BASIC', 'PRO', 'ENTERPRISE')
- [ ] 2.2 Создать `src/lib/validations/account-schemas.ts` с Zod схемами:
  - `createAccountSchema`
  - `updateAccountSchema`
  - `assignManagerSchema`
  - `transferAccountSchema`
  - `acceptTransferSchema`
- [ ] 2.3 Создать `src/types/accounts/interfaces.ts` с интерфейсами:
  - `UserAccountWithRelations`
  - `AccountManagerWithUser`
  - `TariffPlanWithFeatures`

**Критерии завершения:**

- [ ] Все типы экспортируются
- [ ] Zod схемы валидируют входные данные
- [ ] Типы соответствуют Prisma моделям

**Оценка времени:** 0.5 дня

---

### Этап 3: Сервисы и бизнес-логика

**Цель:** Реализовать бизнес-логику работы с аккаунтами

**Задачи:**

- [x] 3.1 Создать `src/services/accounts/TariffPlanService.ts`:
  - `getAllPlans()`
  - `getPlanByCode(code: string)`
  - `getPlanById(id: string)`
- [ ] 3.2 Создать `src/services/accounts/AccountService.ts`:
  - `createAccount(userId, type, tariffPlanCode)`
  - `getUserAccounts(userId)`
  - `getAccountById(accountId, userId)` (с проверкой доступа)
  - `updateAccount(accountId, userId, data)`
  - `deleteAccount(accountId, userId)`
  - `switchAccount(accountId, userId)` (установка текущего аккаунта)
- [ ] 3.3 Создать `src/services/accounts/AccountManagerService.ts`:
  - `assignManager(accountId, ownerId, userId, permissions)`
  - `revokeManager(accountId, ownerId, managerId)`
  - `getManagedAccounts(userId)` (аккаунты, которыми управляет пользователь)
  - `getAccountManagers(accountId, ownerId)`
  - `updateManagerPermissions(accountId, ownerId, managerId, permissions)`
- [ ] 3.4 Создать `src/services/accounts/AccountTransferService.ts`:
  - `requestTransfer(accountId, ownerId, toUserId)`
  - `acceptTransfer(transferId, userId)`
  - `rejectTransfer(transferId, userId, reason)`
  - `cancelTransfer(transferId, userId)`
  - `getTransferRequests(userId)` (входящие и исходящие)
- [x] 3.5 Создать `src/services/accounts/AccountAccessService.ts`:
- [x] 3.6 Создать `src/services/accounts/AccountRulesService.ts` (интеграция с json-rules-engine):
- [x] 3.7 Создать `src/services/workflows/machines/AccountMachine.ts` (XState машина):
- [x] 3.8 Создать `src/services/workflows/AccountWorkflowService.ts` (интеграция с XState):
- [x] 3.9 Создать `src/services/rules/rules/account-rules.ts` (бизнес-правила):
  - `canAccessAccount(userId, accountId)` (проверка доступа)
  - `canManageAccount(userId, accountId)` (проверка прав управления)
  - `canEditAccount(userId, accountId)`
  - `canDeleteAccount(userId, accountId)`
  - `getCurrentAccount(userId)` (из сессии/контекста)
  - `setCurrentAccount(userId, accountId)`

**Критерии завершения:**

- [ ] Все методы реализованы
- [ ] Проверки доступа работают корректно
- [ ] Обработка ошибок реализована
- [ ] Логирование операций

**Оценка времени:** 2 дня

---

### Этап 4: API Endpoints

**Цель:** Создать REST API для работы с аккаунтами

**Задачи:**

- [ ] 4.1 Создать `src/app/api/accounts/route.ts`:
  - `GET /api/accounts` - список аккаунтов пользователя
  - `POST /api/accounts` - создание аккаунта
- [ ] 4.2 Создать `src/app/api/accounts/[id]/route.ts`:
  - `GET /api/accounts/[id]` - получение аккаунта
  - `PUT /api/accounts/[id]` - обновление аккаунта
  - `DELETE /api/accounts/[id]` - удаление аккаунта
- [ ] 4.3 Создать `src/app/api/accounts/[id]/switch/route.ts`:
  - `POST /api/accounts/[id]/switch` - переключение на аккаунт
- [ ] 4.4 Создать `src/app/api/accounts/current/route.ts`:
  - `GET /api/accounts/current` - получение текущего аккаунта
- [ ] 4.5 Создать `src/app/api/accounts/[id]/managers/route.ts`:
  - `GET /api/accounts/[id]/managers` - список менеджеров
  - `POST /api/accounts/[id]/managers` - назначение менеджера
- [ ] 4.6 Создать `src/app/api/accounts/[id]/managers/[managerId]/route.ts`:
  - `PUT /api/accounts/[id]/managers/[managerId]` - обновление прав
  - `DELETE /api/accounts/[id]/managers/[managerId]` - отзыв прав
- [ ] 4.7 Создать `src/app/api/accounts/[id]/transfer/route.ts`:
  - `POST /api/accounts/[id]/transfer` - запрос на передачу
  - `GET /api/accounts/[id]/transfer` - статус передачи
- [ ] 4.8 Создать `src/app/api/accounts/transfers/[transferId]/route.ts`:
  - `POST /api/accounts/transfers/[transferId]/accept` - принять передачу
  - `POST /api/accounts/transfers/[transferId]/reject` - отклонить передачу
  - `POST /api/accounts/transfers/[transferId]/cancel` - отменить передачу
- [ ] 4.9 Создать `src/app/api/tariff-plans/route.ts`:
  - `GET /api/tariff-plans` - список тарифных планов
- [ ] 4.10 Обновить `src/app/api/register/route.ts`:
  - Добавить выбор типа аккаунта при регистрации
  - Автоматическое создание аккаунта с тарифом FREE

**Критерии завершения:**

- [ ] Все endpoints реализованы
- [ ] Валидация входных данных
- [ ] Проверка прав доступа
- [ ] Обработка ошибок
- [ ] EventService интеграция для логирования

**Оценка времени:** 2 дня

---

### Этап 5: Контекст и состояние

**Цель:** Создать React контекст для управления текущим аккаунтом

**Задачи:**

- [ ] 5.1 Создать `src/contexts/AccountContext.tsx`:
  - `currentAccount` - текущий выбранный аккаунт
  - `userAccounts` - список всех аккаунтов пользователя
  - `switchAccount(accountId)` - переключение аккаунта
  - `refreshAccounts()` - обновление списка
- [ ] 5.2 Создать `src/hooks/useAccount.ts`:
  - Хук для использования контекста аккаунта
  - `useCurrentAccount()` - получение текущего аккаунта
  - `useUserAccounts()` - получение списка аккаунтов
  - `useSwitchAccount()` - переключение аккаунта
- [ ] 5.3 Интегрировать контекст в `src/app/layout.tsx` или корневой layout
- [ ] 5.4 Сохранение текущего аккаунта в localStorage/sessionStorage
- [ ] 5.5 Восстановление текущего аккаунта при загрузке страницы

**Критерии завершения:**

- [ ] Контекст работает корректно
- [ ] Состояние сохраняется между перезагрузками
- [ ] Нет лишних ре-рендеров

**Оценка времени:** 1 день

---

### Этап 6: UI компоненты - Хедер и переключение аккаунтов

**Цель:** Создать компонент переключения аккаунтов в хедере

**Задачи:**

- [ ] 6.1 Создать `src/components/accounts/AccountSwitcher.tsx`:
  - Выпадающий список аккаунтов (MUI Menu/Select)
  - Отображение текущего аккаунта
  - Иконка для открытия списка
  - Быстрое переключение
- [ ] 6.2 Интегрировать в `src/components/layout/horizontal/Header.tsx` или вертикальный хедер
- [ ] 6.3 Стилизация в стиле MUI
- [ ] 6.4 Отображение типа аккаунта и тарифа
- [ ] 6.5 Индикатор текущего аккаунта

**Критерии завершения:**

- [ ] Компонент отображается в хедере
- [ ] Переключение работает корректно
- [ ] Стили соответствуют дизайну MUI

**Оценка времени:** 1 день

---

### Этап 7: UI компоненты - Управление аккаунтами

**Цель:** Создать страницы и компоненты для управления аккаунтами

**Задачи:**

- [ ] 7.1 Создать `src/app/[lang]/(dashboard)/(private)/accounts/page.tsx`:
  - Список всех аккаунтов пользователя
  - Карточки аккаунтов с информацией
  - Кнопки управления
- [ ] 7.2 Создать `src/app/[lang]/(dashboard)/(private)/accounts/[id]/page.tsx`:
  - Детальная информация об аккаунте
  - Редактирование аккаунта
  - Управление тарифом
- [ ] 7.3 Создать `src/app/[lang]/(dashboard)/(private)/accounts/[id]/managers/page.tsx`:
  - Список назначенных менеджеров
  - Форма назначения нового менеджера
  - Управление правами
  - Отзыв прав
- [ ] 7.4 Создать `src/app/[lang]/(dashboard)/(private)/accounts/transfers/page.tsx`:
  - Список запросов на передачу (входящие и исходящие)
  - Принятие/отклонение запросов
- [ ] 7.5 Создать `src/components/accounts/AccountCard.tsx`:
  - Карточка аккаунта для списка
  - Информация о тарифе
  - Статус аккаунта
- [ ] 7.6 Создать `src/components/accounts/AccountForm.tsx`:
  - Форма создания/редактирования аккаунта
- [ ] 7.7 Создать `src/components/accounts/ManagerList.tsx`:
  - Список менеджеров с правами
- [ ] 7.8 Создать `src/components/accounts/AssignManagerDialog.tsx`:
  - Диалог назначения менеджера
  - Поиск пользователя
  - Выбор прав
- [ ] 7.9 Создать `src/components/accounts/TransferAccountDialog.tsx`:
  - Диалог запроса на передачу аккаунта
  - Поиск пользователя
  - Подтверждение

**Критерии завершения:**

- [ ] Все страницы созданы
- [ ] Компоненты работают корректно
- [ ] Валидация форм
- [ ] Обработка ошибок
- [ ] Уведомления об успешных операциях

**Оценка времени:** 3 дня

---

### Этап 8: Интеграция с регистрацией

**Цель:** Добавить выбор типа аккаунта при регистрации

**Задачи:**

- [ ] 8.1 Обновить `src/views/Register.tsx`:
  - Добавить выбор типа аккаунта (радио-кнопки или карточки)
  - Три варианта: "Для публикации объявления", "Для размещения компании", "Сеть компаний"
  - Визуальное оформление выбора
- [ ] 8.2 Обновить `src/app/api/register/route.ts`:
  - Принять `accountType` в теле запроса
  - После создания пользователя создать аккаунт с типом и тарифом FREE
  - Установить созданный аккаунт как текущий
- [ ] 8.3 Обновить валидацию регистрации:
  - Добавить `accountType` в схему валидации
  - Проверка допустимых значений
- [ ] 8.4 Обновить `src/lib/validations/registration-schemas.ts`:
  - Добавить поле `accountType` в схему регистрации

**Критерии завершения:**

- [ ] Выбор типа аккаунта работает при регистрации
- [ ] Аккаунт создается автоматически
- [ ] Валидация корректна

**Оценка времени:** 1 день

---

### Этап 9: Навигация и меню - Блок Accounts

**Цель:** Создать блок Accounts в меню навигации со всеми страницами управления аккаунтами и тарифами

**Задачи:**

- [ ] 9.1 Обновить `src/data/navigation/verticalMenuData.tsx`:
  - Добавить блок "Accounts" с иконкой
  - Подменю:
    - "Мои аккаунты" → `/accounts` (список всех аккаунтов)
    - "Тарифные планы" → `/accounts/tariffs` (список тарифов)
    - "Менеджеры" → `/accounts/managers` (управление менеджерами всех аккаунтов)
    - "Передача аккаунтов" → `/accounts/transfers` (входящие/исходящие запросы)
    - "Создать аккаунт" → `/accounts/create` (форма создания)
- [ ] 9.2 Обновить `src/data/navigation/horizontalMenuData.tsx`:
  - Аналогичный блок "Accounts" для горизонтального меню
- [ ] 9.3 Создать страницу тарифов `src/app/[lang]/(dashboard)/(private)/accounts/tariffs/page.tsx`:
  - Список всех доступных тарифных планов
  - Сравнение функционала
  - Возможность смены тарифа для аккаунта
- [ ] 9.4 Создать страницу менеджеров `src/app/[lang]/(dashboard)/(private)/accounts/managers/page.tsx`:
  - Сводная таблица всех менеджеров по всем аккаунтам
  - Быстрое управление правами
- [ ] 9.5 Создать страницу создания аккаунта `src/app/[lang]/(dashboard)/(private)/accounts/create/page.tsx`:
  - Форма создания нового аккаунта
  - Выбор типа и названия
- [ ] 9.6 Условное отображение пунктов меню:
  - Показывать блок только авторизованным пользователям
  - "Менеджеры" и "Создать аккаунт" только для NETWORK аккаунтов
  - "Передача аккаунтов" только при наличии запросов

**Критерии завершения:**

- [ ] Блок Accounts добавлен в вертикальное меню
- [ ] Блок Accounts добавлен в горизонтальное меню
- [ ] Все страницы доступны из меню
- [ ] Навигация работает корректно
- [ ] Условное отображение реализовано

**Оценка времени:** 1.5 дня

---

### Этап 10: Уведомления и события

**Цель:** Интегрировать систему аккаунтов с EventService и уведомлениями

**Задачи:**

- [ ] 10.1 Добавить события в `src/services/events/EventService.ts`:
  - `account.created`
  - `account.updated`
  - `account.deleted`
  - `account.switched`
  - `manager.assigned`
  - `manager.revoked`
  - `transfer.requested`
  - `transfer.accepted`
  - `transfer.rejected`
- [ ] 10.2 Создать уведомления для:
  - Назначение менеджером аккаунта
  - Запрос на передачу аккаунта
  - Принятие/отклонение передачи
  - Отзыв прав управления
- [ ] 10.3 Интегрировать с `NotificationScenario`:
  - Сценарии для событий аккаунтов
  - Email/SMS уведомления

**Критерии завершения:**

- [ ] Все события логируются
- [ ] Уведомления отправляются
- [ ] Сценарии настроены

**Оценка времени:** 1 день

---

### Этап 11: Тестирование

**Цель:** Написать тесты для системы аккаунтов

**Задачи:**

- [ ] 11.1 Unit тесты для сервисов:
  - `AccountService.test.ts`
  - `AccountManagerService.test.ts`
  - `AccountTransferService.test.ts`
  - `AccountAccessService.test.ts`
- [ ] 11.2 Unit тесты для API:
  - `accounts-route.test.ts`
  - `account-managers-route.test.ts`
  - `account-transfer-route.test.ts`
- [ ] 11.3 E2E тесты:
  - Регистрация с выбором типа аккаунта
  - Создание аккаунта
  - Назначение менеджера
  - Передача аккаунта
  - Переключение аккаунтов

**Критерии завершения:**

- [ ] Покрытие тестами > 80%
- [ ] Все тесты проходят
- [ ] E2E тесты проверяют основные сценарии

**Оценка времени:** 2 дня

---

### Этап 12: Документация

**Цель:** Создать документацию для системы аккаунтов

**Задачи:**

- [ ] 12.1 Обновить `ROOT_FILES_DESCRIPTION.md`:
  - Добавить раздел о модуле аккаунтов
  - Описать структуру файлов
  - Описать API endpoints
- [ ] 12.2 Создать `docs/modules/accounts/README.md`:
  - Описание модуля
  - Архитектура
  - Примеры использования
- [ ] 12.3 Создать `docs/api/accounts.md`:
  - Документация API endpoints
  - Примеры запросов/ответов
- [ ] 12.4 Обновить `docs/database/database.md`:
  - Добавить описание новых моделей

**Критерии завершения:**

- [ ] Документация полная и актуальная
- [ ] Примеры работают
- [ ] Структура соответствует стандартам проекта

**Оценка времени:** 1 день

---

## 🔄 Интеграция с существующими модулями

### Модуль регистрации
- Обновить форму регистрации для выбора типа аккаунта
- Автоматическое создание аккаунта после регистрации

### Модуль ролей
- Проверка прав доступа к аккаунтам
- Роли для управления аккаунтами

### Модуль уведомлений
- Уведомления о назначении менеджером
- Уведомления о запросах на передачу
- Уведомления о принятии/отклонении передачи

### Модуль событий
- Логирование всех операций с аккаунтами
- Метрики использования аккаунтов

---

## 📝 Seed данные

### Этап 13: Демо-данные для аккаунтов

**Цель:** Наполнить базу демо-данными для тестирования системы аккаунтов

**Задачи:**

- [ ] 13.1 Обновить `prisma/seed.ts` с демо-данными:
  - Тарифные планы (уже создано):
    - `FREE` - бесплатный тариф (по умолчанию)
    - `BASIC` - базовый тариф
    - `PRO` - профессиональный тариф
    - `ENTERPRISE` - корпоративный тариф
  - Демо-аккаунты:
    - Для `admin@example.com`: NETWORK аккаунт с PRO тарифом
    - Для `user@example.com`: LISTING аккаунт с FREE тарифом
    - Для `moderator@example.com`: COMPANY аккаунт с BASIC тарифом
    - Для `editor@example.com`: LISTING аккаунт с FREE тарифом (назначен менеджером)
  - Демо-менеджеры:
    - `editor@example.com` назначен менеджером аккаунта `admin@example.com`
    - С правами: canView, canEdit (без canManage, canTransfer)
  - Демо-запросы на передачу:
    - Запрос от `moderator@example.com` к `user@example.com` (pending)

**Критерии завершения:**

- [ ] Все демо-данные создаются при `npx prisma db seed`
- [ ] Демо-пользователи имеют аккаунты
- [ ] Менеджеры назначены корректно
- [ ] Запросы на передачу созданы

**Оценка времени:** 0.5 дня

**Таблица демо-данных:**

| Пользователь | Email | Тип аккаунта | Тариф | Роль в системе |
|--------------|-------|--------------|-------|----------------|
| Admin | admin@example.com | NETWORK | PRO | Владелец с менеджером |
| User | user@example.com | LISTING | FREE | Владелец |
| Moderator | moderator@example.com | COMPANY | BASIC | Владелец, инициатор передачи |
| Editor | editor@example.com | LISTING | FREE | Владелец + менеджер чужого |

**Запуск seed:**
```bash
npx prisma db seed
```

---

## 🚨 Важные замечания

1. **Безопасность:**
   - Все операции с аккаунтами требуют аутентификации
   - Проверка прав доступа на каждом endpoint
   - Валидация входных данных

2. **Производительность:**
   - Индексы на часто используемых полях
   - Кэширование списка аккаунтов пользователя
   - Оптимизация запросов с relations

3. **Масштабируемость:**
   - Учет ограничений тарифных планов
   - Ограничение количества аккаунтов для FREE тарифа
   - Ограничение количества менеджеров

4. **Совместимость:**
   - Существующая модель `Account` (OAuth) не изменяется
   - Новая модель `UserAccount` для бизнес-аккаунтов
   - Обратная совместимость с существующими пользователями

---

## ✅ Критерии приемки

- [ ] Пользователь может выбрать тип аккаунта при регистрации
- [ ] Аккаунт создается автоматически с тарифом FREE
- [ ] Пользователь может иметь несколько аккаунтов
- [ ] Тарифные планы привязаны к аккаунтам
- [ ] Реализована система назначения менеджеров
- [ ] Реализована система передачи аккаунтов
- [ ] Работает переключение аккаунтов в хедере
- [ ] Все API endpoints работают корректно
- [ ] UI компоненты созданы и работают
- [ ] Тесты написаны и проходят
- [ ] Документация обновлена

---

## 📊 Общая оценка времени

- **Этап 1:** 1 день
- **Этап 2:** 0.5 дня
- **Этап 3:** 2 дня
- **Этап 4:** 2 дня
- **Этап 5:** 1 день
- **Этап 6:** 1 день
- **Этап 7:** 3 дня
- **Этап 8:** 1 день
- **Этап 9:** 0.5 дня
- **Этап 10:** 1 день
- **Этап 11:** 2 дня
- **Этап 12:** 1 день

**Итого:** ~16.5 дней (~3 недели)

---

## 🔗 Связанные задачи

- Интеграция с модулем тарифных планов (если будет создан отдельный модуль)
- Интеграция с модулем платежей (для оплаты тарифов)
- Интеграция с модулем объявлений (привязка к аккаунту типа LISTING)
- Интеграция с модулем компаний (привязка к аккаунту типа COMPANY)

---

**Примечание:** Этот план является живым документом и может обновляться по мере реализации и выявления новых требований.

