# План: Модуль Firecrawl Web Scraper

**Дата создания:** 2025-11-28  
**Статус:** 🟡 Активный  
**Приоритет:** Средний

---

## 📌 Обзор

**Цель:** Добавить интеграцию с [Firecrawl API](https://www.firecrawl.dev/) для парсинга сайтов и извлечения структурированных данных о компаниях.

**Расположение UI:** `/admin/settings/services` (конфигурация) + `/admin/tools/web-scraper` (интерфейс парсинга)

**Этапы:**
1. **Этап 1:** Интеграция сервиса + UI для парсинга с выводом результатов
2. **Этап 2:** Сохранение результатов в БД

---

## 🏗️ Архитектура модуля

```
┌────────────────────────────────────────────────────────────────────┐
│                         UI Layer                                    │
│                                                                    │
│  /admin/settings/services     /admin/tools/web-scraper             │
│  ┌──────────────────────┐    ┌──────────────────────────────────┐ │
│  │ + FIRECRAWL сервис   │    │ [URL input] [🔍 Парсить]         │ │
│  │   в таблице          │    │                                   │ │
│  │                      │    │ Результаты:                       │ │
│  └──────────────────────┘    │ - Название компании               │ │
│                               │ - Телефоны, Email                 │ │
│                               │ - Соцсети, Адрес                  │ │
│                               │ - Режим работы                    │ │
│                               │ - Логотип, Фото                   │ │
│                               │ - Категория, Услуги               │ │
│                               └──────────────────────────────────┘ │
└──────────────────────────────────┬─────────────────────────────────┘
                                   │
┌──────────────────────────────────▼─────────────────────────────────┐
│                         API Layer                                   │
│                                                                    │
│  /api/admin/settings/services/*    /api/admin/tools/web-scraper    │
│  (существующие endpoints)          POST { url, options }            │
└──────────────────────────────────┬─────────────────────────────────┘
                                   │
┌──────────────────────────────────▼─────────────────────────────────┐
│                       Service Layer                                 │
│                                                                    │
│  FirecrawlConnector.ts            WebScraperService.ts             │
│  - testConnection()               - scrapeWebsite(url)             │
│  - getClient()                    - extractCompanyData()           │
│                                   - parseContacts()                │
└──────────────────────────────────┬─────────────────────────────────┘
                                   │
                                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                    Firecrawl API (external)                        │
│                    https://api.firecrawl.dev/v1                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура файлов

### Этап 1: Базовая интеграция

| Путь | Назначение |
|------|------------|
| **Типы и конфиги** |
| `src/lib/config/types.ts` | + FIRECRAWL в ServiceType |
| `src/lib/validations/firecrawl-schemas.ts` | Zod схемы для Firecrawl |
| **Коннектор** |
| `src/modules/settings/services/connectors/FirecrawlConnector.ts` | Коннектор для тестирования API |
| `src/modules/settings/services/connectors/index.ts` | + экспорт FirecrawlConnector |
| **Сервис парсинга** |
| `src/services/web-scraper/WebScraperService.ts` | Основной сервис парсинга |
| `src/services/web-scraper/types.ts` | Типы для результатов парсинга |
| `src/services/web-scraper/extractors/ContactExtractor.ts` | Извлечение контактов |
| `src/services/web-scraper/extractors/SocialExtractor.ts` | Извлечение соцсетей |
| `src/services/web-scraper/extractors/MediaExtractor.ts` | Извлечение логотипа/фото |
| `src/services/web-scraper/index.ts` | Экспорты |
| **API endpoints** |
| `src/app/api/admin/tools/web-scraper/route.ts` | POST - парсинг сайта |
| `src/app/api/admin/tools/web-scraper/batch/route.ts` | POST - пакетный парсинг |
| **UI страницы** |
| `src/app/[lang]/(dashboard)/(private)/admin/tools/web-scraper/page.tsx` | Страница парсинга |
| `src/views/admin/tools/web-scraper/WebScraperForm.tsx` | Форма ввода URL |
| `src/views/admin/tools/web-scraper/ScrapedDataDisplay.tsx` | Отображение результатов |
| `src/views/admin/tools/web-scraper/CompanyCard.tsx` | Карточка компании |
| **Навигация** |
| `src/data/navigation/verticalMenuData.tsx` | + пункт меню |
| `src/data/navigation/horizontalMenuData.tsx` | + пункт меню |
| **Переводы** |
| `src/data/dictionaries/en.json` | + ключи для web-scraper |
| `src/data/dictionaries/ru.json` | + ключи для web-scraper |

### Этап 2: Сохранение в БД

| Путь | Назначение |
|------|------------|
| **Prisma** |
| `prisma/schema.prisma` | + модели ScrapedCompany, ScrapedContact |
| `prisma/seed.ts` | + seed данные |
| **API** |
| `src/app/api/admin/tools/web-scraper/results/route.ts` | GET/POST результаты |
| `src/app/api/admin/tools/web-scraper/results/[id]/route.ts` | GET/PUT/DELETE |
| **UI** |
| `src/views/admin/tools/web-scraper/SavedResultsList.tsx` | Список сохранённых |
| `src/views/admin/tools/web-scraper/ResultDetailDialog.tsx` | Детали результата |

---

## 📊 Модели данных

### ServiceConfiguration (существующая, добавить тип)

```typescript
// src/lib/config/types.ts
export const ServiceType = {
  // ... existing
  FIRECRAWL: 'FIRECRAWL'  // ← ДОБАВИТЬ
} as const
```

### ScrapedCompany (Этап 2)

```prisma
model ScrapedCompany {
  id              String   @id @default(cuid())
  sourceUrl       String   // URL источника
  
  // Основная информация
  name            String?  // Название компании
  description     String?  // Краткое описание
  category        String?  // Категория (автошины, перевозки и т.д.)
  
  // Контакты
  phones          String?  // JSON: ["phone1", "phone2"]
  emails          String?  // JSON: ["email1", "email2"]
  address         String?
  coordinates     String?  // JSON: { lat, lng }
  
  // Соцсети
  socialLinks     String?  // JSON: { vk, telegram, whatsapp, instagram, facebook }
  website         String?
  
  // Время работы
  workingHours    String?  // JSON: { mon: "9:00-18:00", ... }
  
  // Медиа
  logoUrl         String?
  photos          String?  // JSON: ["url1", "url2"]
  
  // Услуги
  services        String?  // JSON: ["услуга1", "услуга2"]
  
  // Мета
  rawData         String?  // Полный JSON от Firecrawl
  scrapedAt       DateTime @default(now())
  scrapedBy       String?
  status          String   @default("new") // new, verified, rejected
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([sourceUrl])
  @@index([category])
  @@index([status])
  @@map("scraped_companies")
}
```

### Типы результатов парсинга

```typescript
// src/services/web-scraper/types.ts

export interface ScrapedCompanyData {
  // Основное
  name: string | null
  description: string | null
  category: string | null
  
  // Контакты
  phones: string[]
  emails: string[]
  address: string | null
  coordinates: { lat: number; lng: number } | null
  
  // Соцсети
  socialLinks: {
    vk?: string
    telegram?: string
    whatsapp?: string
    instagram?: string
    facebook?: string
    youtube?: string
    tiktok?: string
  }
  website: string | null
  
  // Режим работы
  workingHours: Record<string, string> | null
  
  // Медиа
  logoUrl: string | null
  photos: string[]
  
  // Услуги
  services: string[]
  
  // Мета
  sourceUrl: string
  scrapedAt: Date
  confidence: number // 0-100, уверенность в данных
}

export interface ScrapeOptions {
  extractImages?: boolean
  maxDepth?: number  // Глубина обхода (для crawl)
  timeout?: number
  waitForSelector?: string
}

export interface ScrapeResult {
  success: boolean
  data: ScrapedCompanyData | null
  rawMarkdown?: string
  error?: string
  duration: number
}
```

---

## 🔌 API Endpoints

### POST `/api/admin/tools/web-scraper`

**Request:**
```json
{
  "url": "https://example-company.ru",
  "options": {
    "extractImages": true,
    "maxDepth": 2
  }
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "name": "ООО Пример",
    "description": "Компания занимается продажей автошин...",
    "category": "Автошины и диски",
    "phones": ["+7 (999) 123-45-67", "8 800 555-35-35"],
    "emails": ["info@example.ru", "sales@example.ru"],
    "address": "г. Москва, ул. Примерная, д. 1",
    "coordinates": { "lat": 55.7558, "lng": 37.6173 },
    "socialLinks": {
      "vk": "https://vk.com/example",
      "telegram": "https://t.me/example",
      "instagram": "https://instagram.com/example"
    },
    "workingHours": {
      "mon": "09:00-18:00",
      "tue": "09:00-18:00",
      "sat": "10:00-16:00",
      "sun": "выходной"
    },
    "logoUrl": "https://example.ru/logo.png",
    "photos": ["https://example.ru/photo1.jpg"],
    "services": ["Продажа шин", "Шиномонтаж", "Хранение шин"],
    "sourceUrl": "https://example-company.ru",
    "confidence": 85
  },
  "duration": 2340
}
```

---

## 🎨 UI компоненты

### Страница Web Scraper

```
┌────────────────────────────────────────────────────────────────────┐
│  🔍 Web Scraper                                    [История (12)]   │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  URL сайта:                                                        │
│  ┌────────────────────────────────────────┐  ┌──────────────────┐ │
│  │ https://example-company.ru             │  │ 🔍 Парсить      │ │
│  └────────────────────────────────────────┘  └──────────────────┘ │
│                                                                    │
│  ☐ Извлекать изображения  ☐ Глубокий обход (crawl)                │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  📊 Результаты парсинга                          Уверенность: 85%  │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  🏢 ООО "Пример"                              [Сохранить]   │  │
│  │  ─────────────────────────────────────────────────────────  │  │
│  │  📝 Категория: Автошины и диски                             │  │
│  │  📍 г. Москва, ул. Примерная, д. 1                          │  │
│  │                                                              │  │
│  │  📞 Телефоны:           📧 Email:                            │  │
│  │  +7 (999) 123-45-67     info@example.ru                      │  │
│  │  8 800 555-35-35        sales@example.ru                     │  │
│  │                                                              │  │
│  │  🌐 Соцсети:                                                 │  │
│  │  [VK] [Telegram] [Instagram]                                 │  │
│  │                                                              │  │
│  │  🕐 Режим работы:                                            │  │
│  │  Пн-Пт: 09:00-18:00, Сб: 10:00-16:00, Вс: выходной           │  │
│  │                                                              │  │
│  │  🛠️ Услуги: Продажа шин, Шиномонтаж, Хранение шин            │  │
│  │                                                              │  │
│  │  🖼️ Логотип:           📷 Фото:                              │  │
│  │  [image]               [gallery...]                          │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ 📋 Raw JSON                                        [Copy]    │ │
│  │ { "name": "ООО Пример", ... }                                │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📝 Задачи по этапам

### Этап 1: Базовая интеграция (5-7 часов)

| # | Задача | Файлы | Время | Статус |
|---|--------|-------|-------|--------|
| 1.1 | Добавить FIRECRAWL в ServiceType | `types.ts` | 10 мин | ⬜ |
| 1.2 | Создать FirecrawlConnector | `FirecrawlConnector.ts` | 30 мин | ⬜ |
| 1.3 | Добавить FIRECRAWL в UI страницы services | `page.tsx` (services) | 20 мин | ⬜ |
| 1.4 | Создать типы для web-scraper | `types.ts` | 30 мин | ⬜ |
| 1.5 | Создать WebScraperService | `WebScraperService.ts` | 1.5 часа | ⬜ |
| 1.6 | Создать API endpoint | `route.ts` | 45 мин | ⬜ |
| 1.7 | Создать UI страницу парсинга | `page.tsx`, компоненты | 2 часа | ⬜ |
| 1.8 | Добавить навигацию | `*MenuData.tsx` | 15 мин | ⬜ |
| 1.9 | Добавить переводы | `en.json`, `ru.json` | 15 мин | ⬜ |
| 1.10 | Тестирование | - | 30 мин | ⬜ |

### Этап 2: Сохранение в БД (3-4 часа)

| # | Задача | Файлы | Время | Статус |
|---|--------|-------|-------|--------|
| 2.1 | Добавить модель ScrapedCompany в Prisma | `schema.prisma` | 20 мин | ⬜ |
| 2.2 | Миграция БД | `prisma migrate` | 10 мин | ⬜ |
| 2.3 | API для сохранения результатов | `results/route.ts` | 45 мин | ⬜ |
| 2.4 | UI для списка сохранённых | `SavedResultsList.tsx` | 1 час | ⬜ |
| 2.5 | UI для деталей результата | `ResultDetailDialog.tsx` | 45 мин | ⬜ |
| 2.6 | Интеграция с основной страницей | - | 30 мин | ⬜ |
| 2.7 | Seed данные | `seed.ts` | 15 мин | ⬜ |

---

## 🔐 Безопасность

1. **API Key** — хранится зашифрованным в ServiceConfiguration
2. **Rate Limiting** — использовать существующий RateLimitEngine
3. **Валидация URL** — проверка на допустимые домены
4. **Права доступа** — только ADMIN и SUPERADMIN

---

## 📊 Метрики (Prometheus)

```typescript
// src/lib/metrics/web-scraper.ts
web_scraper_requests_total     // Counter: всего запросов
web_scraper_success_total      // Counter: успешных
web_scraper_errors_total       // Counter: ошибок
web_scraper_duration_seconds   // Histogram: время парсинга
web_scraper_confidence_score   // Histogram: уверенность результатов
```

---

## 🎯 Критерии готовности

### Этап 1 ✓
- [ ] FIRECRAWL доступен в списке сервисов
- [ ] Можно добавить/редактировать конфигурацию Firecrawl
- [ ] Тест подключения работает
- [ ] Страница `/admin/tools/web-scraper` доступна
- [ ] Можно ввести URL и получить результаты парсинга
- [ ] Результаты отображаются в красивом UI
- [ ] Можно скопировать JSON результата

### Этап 2 ✓
- [ ] Результаты можно сохранить в БД
- [ ] Список сохранённых результатов доступен
- [ ] Можно просмотреть/редактировать/удалить результат
- [ ] Фильтрация по категории, дате, статусу

---

## 📚 Связанные документы

- [Firecrawl API Documentation](https://docs.firecrawl.dev/)
- [Модуль External Services](ROOT_FILES_DESCRIPTION.md#модуль-конфигурация-внешних-сервисов)
- [BaseConnector](../../src/modules/settings/services/connectors/BaseConnector.ts)

---

## 📝 История изменений

| Дата | Изменение |
|------|-----------|
| 2025-11-28 | Создание плана |


