# План рефакторинга ролей для поддержки переименования

**Дата создания:** 2025-11-25  
**Статус:** ✅ Выполнен  
**Приоритет:** Высокий  
**Завершено:** 2025-11-25

---

## 🎯 Цель

Обеспечить возможность **переименования ролей без влияния на работу системы** путём:

1. Добавления неизменяемого поля `code` для идентификации системных ролей
2. Добавления поля `level` для определения иерархии
3. Добавления поля `isSystem` для защиты системных ролей
4. Рефакторинга всех проверок с использования имён на использование `code`/`level`/`isSystem`

---

## 📋 Связанные документы

- [Анализ: Рефакторинг ролей для переименования](../../analysis/architecture/analysis-roles-renaming-refactoring-2025-11-25.md)
- [Документация модуля ролей](../../ROOT_FILES_DESCRIPTION.md)
- [Инструкция для AI](../../AI_WORKFLOW_GUIDE.md)

---

## ⏱️ Сроки

- **Начало:** 2025-11-25
- **Планируемое окончание:** 2025-11-28
- **Оценка трудозатрат:** 12-16 часов
- **Фактическое окончание:** (заполняется после завершения)

---

## 📊 Этапы реализации

### Этап 1: Миграция базы данных
**Цель:** Добавить новые поля в модель Role

**Задачи:**
- [ ] Обновить `prisma/schema.prisma`:
  ```prisma
  model Role {
    id          String   @id @default(cuid())
    code        String   @unique  // Неизменяемый код: 'SUPERADMIN', 'ADMIN'
    name        String   @unique  // Отображаемое имя (можно менять)
    description String?
    permissions String?  @default("{}")
    level       Int      @default(100)  // Уровень иерархии (0 = высший)
    isSystem    Boolean  @default(false) // Системная роль
    users       User[]
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
  }
  ```
- [ ] Создать миграцию Prisma: `npx prisma migrate dev --name add_role_code_level_system`
- [ ] Создать скрипт миграции данных `src/scripts/migrate-roles-code.ts`:
  - Заполнить `code` = `name.toUpperCase()` для существующих ролей
  - Установить `level` согласно текущей иерархии
  - Установить `isSystem = true` для системных ролей

**Данные для seed/миграции:**

| code | name | level | isSystem |
|------|------|-------|----------|
| SUPERADMIN | Superadmin | 0 | true |
| ADMIN | Admin | 10 | true |
| MANAGER | Manager | 20 | true |
| EDITOR | Editor | 30 | true |
| MODERATOR | Moderator | 40 | true |
| SEO | SEO | 50 | true |
| MARKETOLOG | Marketolog | 60 | true |
| SUPPORT | Support | 70 | true |
| SUBSCRIBER | Subscriber | 80 | true |
| USER | User | 90 | true |

**Критерии завершения:**
- [ ] Миграция создана и применена
- [ ] Все существующие роли имеют заполненные `code`, `level`, `isSystem`
- [ ] База данных стабильна

**Оценка времени:** 1-2 часа

---

### Этап 2: Обновление типов TypeScript
**Цель:** Добавить новые поля в интерфейсы

**Задачи:**
- [ ] Обновить `src/utils/permissions/permissions.ts`:
  ```typescript
  export interface Role {
    id: string
    code: string           // NEW
    name: string
    description?: string | null
    permissions?: string | null
    level: number          // NEW
    isSystem: boolean      // NEW
  }
  ```
- [ ] Обновить `src/types/prisma.d.ts` (если есть)
- [ ] Обновить `src/lib/role-cache/types.ts`
- [ ] Проверить и обновить все места использования типа `Role`

**Критерии завершения:**
- [ ] Все типы обновлены
- [ ] TypeScript компилируется без ошибок
- [ ] Автодополнение работает корректно

**Оценка времени:** 1 час

---

### Этап 3: Рефакторинг утилит иерархии
**Цель:** Заменить проверки по имени на проверки по `code`/`level`

**Задачи:**
- [ ] Обновить `src/utils/formatting/string.ts`:
  ```typescript
  // БЫЛО
  export const ROLE_HIERARCHY = ['superadmin', 'admin', ...] as const
  
  // СТАНЕТ (deprecated, для обратной совместимости)
  export const ROLE_HIERARCHY_LEGACY = ['superadmin', 'admin', ...] as const
  
  // NEW: Получение уровня из объекта роли
  export const getRoleLevelFromRole = (role: { level: number } | null): number => {
    return role?.level ?? 100
  }
  
  // NEW: Проверка возможности изменения роли
  export const canModifyRoleByLevel = (actorLevel: number, targetLevel: number): boolean => {
    return actorLevel < targetLevel
  }
  ```
- [ ] Обновить `src/shared/config/protected-roles.ts`:
  ```typescript
  // БЫЛО
  export const isProtectedRole = (roleName: string): boolean => {
    return PROTECTED_ROLES.includes(roleName.toLowerCase())
  }
  
  // СТАНЕТ
  export const isProtectedRole = (role: { isSystem: boolean } | null): boolean => {
    return role?.isSystem ?? false
  }
  
  // Legacy fallback (для переходного периода)
  export const isProtectedRoleByName = (roleName: string): boolean => {
    return PROTECTED_ROLES.includes(roleName.toLowerCase())
  }
  ```
- [ ] Обновить `src/utils/permissions/permissions.ts`:
  ```typescript
  // БЫЛО
  export const isAdmin = (user: UserWithRole | null): boolean => 
    hasRole(user, 'admin')
  
  // СТАНЕТ
  export const isAdmin = (user: UserWithRole | null): boolean => 
    user?.role?.code === 'ADMIN'
  
  export const isSuperadminByCode = (user: UserWithRole | null): boolean =>
    user?.role?.code === 'SUPERADMIN'
  
  export const hasRoleCode = (user: UserWithRole | null, code: string): boolean =>
    user?.role?.code === code
  ```

**Критерии завершения:**
- [ ] Все утилиты обновлены
- [ ] Legacy функции сохранены с пометкой deprecated
- [ ] Новые функции работают корректно

**Оценка времени:** 2-3 часа

---

### Этап 4: Обновление API endpoints
**Цель:** Заменить проверки по имени на проверки по `code`/`level`/`isSystem`

**Файлы для обновления:**

| Файл | Изменения |
|------|-----------|
| `src/app/api/admin/roles/route.ts` | `canModifyRoleByLevel()`, включить `code` в ответ |
| `src/app/api/admin/roles/[id]/route.ts` | `role.isSystem` вместо `isProtectedRole(name)` |
| `src/app/api/admin/users/route.ts` | Проверки по `role.code` |
| `src/app/api/admin/users/[id]/route.ts` | `canModifyRoleByLevel()` |
| `src/app/api/auth/login/route.ts` | Включить `code`, `level` в сессию |
| `src/app/api/user/profile/route.ts` | Включить `code`, `level` в ответ |

**Задачи:**
- [ ] Для каждого файла:
  - Заменить `role.name === 'superadmin'` на `role.code === 'SUPERADMIN'`
  - Заменить `isProtectedRole(role.name)` на `role.isSystem`
  - Заменить `getRoleLevel(role.name)` на `role.level`
  - Добавить `code`, `level`, `isSystem` в ответы API
- [ ] Обновить сериализацию ролей в ответах

**Критерии завершения:**
- [ ] Все API endpoints обновлены
- [ ] API возвращает `code`, `level`, `isSystem`
- [ ] Проверки работают по новым полям

**Оценка времени:** 3-4 часа

---

### Этап 5: Обновление фронтенд компонентов
**Цель:** Использовать `code` для идентификации ролей в UI

**Файлы для обновления:**

| Файл | Изменения |
|------|-----------|
| `src/views/apps/roles/RoleCards.tsx` | `role.code` для иконок, `role.isSystem` для защиты |
| `src/components/dialogs/role-dialog/index.tsx` | Запретить изменение `code` для системных ролей |
| `src/views/apps/user/list/UserListTable.tsx` | Использовать `role.code` для фильтров |
| `src/views/apps/user/list/TableFilters.tsx` | Фильтрация по `code` |
| `src/hooks/usePermissions.ts` | `isSuperadminByCode()` |

**Задачи:**
- [ ] Обновить отображение ролей: использовать `name` для показа, `code` для логики
- [ ] Добавить индикатор системных ролей (`isSystem`)
- [ ] Запретить редактирование `code` для системных ролей в диалоге
- [ ] Обновить цветовую схему на основе `code` вместо `name`

**Критерии завершения:**
- [ ] UI корректно отображает роли
- [ ] Системные роли помечены и защищены
- [ ] Переименование работает без потери функциональности

**Оценка времени:** 2-3 часа

---

### Этап 6: Обновление Socket.IO и middleware
**Цель:** Использовать `code` в real-time коммуникации

**Файлы для обновления:**

| Файл | Изменения |
|------|-----------|
| `src/lib/sockets/middleware/auth.ts` | `role.code === 'SUPERADMIN'` |
| `src/lib/sockets/utils/permissions.ts` | Проверки по `code` |
| `src/lib/sockets/utils/jwt.ts` | Включить `code` в токен |

**Задачи:**
- [ ] Обновить JWT payload: добавить `roleCode`, `roleLevel`
- [ ] Обновить проверки в middleware
- [ ] Протестировать WebSocket соединения

**Критерии завершения:**
- [ ] WebSocket авторизация работает
- [ ] Real-time функции используют `code`

**Оценка времени:** 1-2 часа

---

### Этап 7: Тестирование
**Цель:** Убедиться, что рефакторинг не сломал функциональность

**Задачи:**
- [ ] Обновить unit тесты:
  - `tests/unit/permissions/` - новые проверки
  - `tests/unit/api/roles-route.test.ts` - проверки по `code`
- [ ] Добавить тесты на переименование роли:
  - Создать роль с `code: 'TEST_ROLE'`
  - Переименовать `name: 'Test' -> 'Testing'`
  - Проверить, что пользователи с этой ролью сохранили доступ
- [ ] Ручное тестирование:
  - Переименовать роль в UI
  - Проверить иерархию
  - Проверить защиту системных ролей

**Критерии завершения:**
- [ ] Все unit тесты проходят
- [ ] Переименование роли не влияет на пользователей
- [ ] Иерархия работает корректно

**Оценка времени:** 2-3 часа

---

### Этап 8: Документация
**Цель:** Обновить документацию проекта

**Задачи:**
- [ ] Обновить `docs/ROOT_FILES_DESCRIPTION.md`:
  - Добавить описание полей `code`, `level`, `isSystem`
- [ ] Создать миграционный гайд для администраторов:
  - Как работать с новой системой
  - Как создавать кастомные роли
- [ ] Обновить комментарии в коде

**Критерии завершения:**
- [ ] Документация актуальна
- [ ] Миграционный гайд создан

**Оценка времени:** 1 час

---

## 📈 Прогресс

- **Выполнено:** 100%
- **Осталось:** 0%
- **Текущий этап:** Завершён

### Выполненные этапы:
1. ✅ Миграция БД: добавлены поля `code`, `level`, `isSystem` в модель Role
2. ✅ Обновлены TypeScript типы Role во всех файлах (7 файлов)
3. ✅ Рефакторинг утилит: `string.ts`, `protected-roles.ts`, `permissions.ts`
4. ✅ Обновлены API endpoints (roles route.ts, [id]/route.ts)
5. ✅ Обновлены фронтенд компоненты (RoleCards.tsx и др.)
6. ✅ Обновлён Socket.IO middleware для работы с role code
7. ✅ Unit тесты проходят (13 тестов)
8. ✅ Документация обновлена

---

## ⚠️ Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Сломается авторизация при миграции | Высокая | Критическое | Сохранить legacy функции с fallback |
| Потеря данных пользователей | Низкая | Критическое | Backup перед миграцией |
| Несовместимость с фронтендом | Средняя | Высокое | Поэтапное обновление, тестирование |
| Ошибки в WebSocket | Средняя | Среднее | Тестирование real-time функций |

---

## 🔄 Обратная совместимость

Для обеспечения плавного перехода:

1. **Legacy функции** сохраняются с пометкой `@deprecated`
2. **API** возвращает и `name`, и `code`
3. **Fallback** на проверку по имени, если `code` не заполнен

```typescript
// Пример fallback
export const isSuperadmin = (user: UserWithRole | null): boolean => {
  // Новая проверка по code
  if (user?.role?.code === 'SUPERADMIN') return true
  // Legacy fallback по permissions
  if (getUserPermissions(user) === 'all') return true
  // Legacy fallback по имени (deprecated)
  if (user?.role?.name?.toLowerCase() === 'superadmin') return true
  return false
}
```

---

## 🧪 Тестовый план

| Сценарий | Ожидаемый результат |
|----------|---------------------|
| Переименовать роль "Admin" → "Administrator" | Пользователи сохраняют доступ |
| Создать пользователя с ролью | `roleId` указывает на Role |
| Удалить системную роль | Ошибка "Cannot delete system role" |
| Изменить уровень роли | Иерархия обновляется |
| Войти после переименования роли | Авторизация работает |

---

## ✅ Чек-лист завершения

- [ ] Миграция БД выполнена
- [ ] Все типы обновлены
- [ ] Утилиты рефакторированы
- [ ] API endpoints обновлены
- [ ] Фронтенд обновлён
- [ ] WebSocket обновлён
- [ ] Все тесты проходят
- [ ] Документация актуальна
- [ ] Статус обновлён в STATUS_INDEX.md

---

## 📝 Примечания

### Формат кода роли
- Код роли: **UPPER_SNAKE_CASE** (например, `SUPER_ADMIN`, `CONTENT_EDITOR`)
- Код неизменяем после создания для системных ролей
- Кастомные роли: код генерируется из имени при создании

### Уровни иерархии
- 0 = SUPERADMIN (абсолютный приоритет)
- 10-90 = системные роли
- 100+ = кастомные роли (создаются пользователями)

### Системные vs кастомные роли
- `isSystem = true`: нельзя удалить, нельзя изменить `code`
- `isSystem = false`: можно удалить, можно изменить всё

---

**План создан:** 2025-11-25

