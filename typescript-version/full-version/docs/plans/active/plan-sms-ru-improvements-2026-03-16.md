# План доработки модуля SMS.ru

**Дата создания:** 2026-03-16
**Статус:** Завершён
**Приоритет:** Высокий

---

## 🎯 Цель

Устранить все проблемы, найденные в анализе модуля SMS.ru: критическую уязвимость хранения ключа, некорректный permission module, отсутствие метрик и audit-событий, UX-ошибку в UI, и привести модуль в соответствие со стандартами проекта.

---

## 📋 Связанные документы

- [Анализ модуля SMS.ru](../../analysis/architecture/analysis-sms-ru-module-2026-03-16.md)

---

## ⏱️ Сроки

- **Начало:** 2026-03-16
- **Планируемое окончание:** 2026-03-17
- **Фактическое окончание:** 2026-03-16

---

## 📊 Этапы реализации

### Этап 1: Безопасность — защита API-ключа

**Цель:** Исключить попадание API-ключа в git-репозиторий

**Задачи:**

- [x] Исправить баг `getBalance()` в `SMSRuProvider` (уже выполнено)
- [x] Добавить `sms-ru-settings.json` в `.gitignore`
- [x] Проверить, что `smtp-settings.json` тоже в `.gitignore`

**Критерии завершения:**

- [x] `git status` не показывает `sms-ru-settings.json` как untracked
- [x] `sms-ru-settings.json` отсутствует в `git ls-files`

**Оценка времени:** 10 минут

---

### Этап 2: Исправить permission module

**Цель:** Выделить отдельный `smsManagement` модуль прав вместо `smtpManagement`

**Задачи:**

- [x] Обновить `src/app/api/settings/sms-ru/route.ts` — заменить `smtpManagement.read/update` → `smsManagement.read/update`
- [x] Обновить `src/app/api/settings/sms-ru/balance/route.ts` — аналогично
- [x] Обновить `src/app/api/settings/sms-ru/test/route.ts` — аналогично
- [x] Обновить UI `src/views/apps/settings/sms-ru/index.tsx`:
  - Заменить `checkPermission('smtpManagement', 'update/read')` → `checkPermission('smsManagement', ...)`
  - Исправить кнопку баланса: `disabled={!canUpdate || ...}` → `disabled={!canRead || ...}`
- [x] Добавить `smsManagement` в seed-данные ролей (ADMIN и MANAGER)
- [x] Добавить `smsManagement` в `role-dialog/index.tsx`
- [x] Исправить `filterMenu.ts` — `smsRuSettings` теперь проверяет `smsManagement`

**Критерии завершения:**

- [x] Ни один роут не использует `smtpManagement` для SMS-операций
- [x] Пользователь с `smtpManagement.read` не имеет доступа к SMS-настройкам
- [x] Кнопка баланса доступна пользователям с `smsManagement.read`

**Оценка времени:** 30 минут

---

### Этап 3: Audit events при изменении настроек

**Цель:** Логировать изменения конфигурации SMS.ru в EventService

**Задачи:**

- [x] В PUT `/api/settings/sms-ru/route.ts` после успешного сохранения добавить `eventService.record(...)`:
  - `type: 'sms_settings.updated'`
  - `severity: 'info'`
  - `payload`: `{ testMode, senderChanged, apiKeyChanged }` (без самого ключа)

**Критерии завершения:**

- [ ] При изменении настроек SMS.ru появляется событие в `/api/admin/events`
- [ ] Payload не содержит API-ключ

**Оценка времени:** 20 минут

---

### Этап 4: Prometheus метрики

**Цель:** Добавить мониторинг SMS-операций

**Задачи:**

- [x] Использовать существующий `markNotificationSent/Failed('sms', ...)` из `src/lib/metrics/notifications.ts`
- [x] Инкрементировать счётчики в `SMSRuProvider.sendCode()` и `SMSRuProvider.sendTest()`

**Критерии завершения:**

- [ ] Метрики видны на `/api/metrics`
- [ ] После тестовой отправки `sms_sent_total` увеличивается

**Оценка времени:** 40 минут

---

### Этап 5: Улучшение типизации `(result as any)`

**Цель:** Убрать небезопасные касты в `SMSRuProvider`

**Задачи:**

- [x] Объявить локальные интерфейсы для ответов SMS.ru (`SendSmsResponse`, `SmsItemResult`) в `SMSRuProvider.ts`
- [x] Заменить все `(result as any)` на типизированные обращения

**Критерии завершения:**

- [ ] `pnpm lint` не выдаёт `@typescript-eslint/no-explicit-any` для этого файла
- [ ] TypeScript strict mode компилирует без ошибок

**Оценка времени:** 30 минут

---

### Этап 6: i18n для UI

**Цель:** Перевести все строки UI через словари проекта

**Задачи:**

- [x] Добавить ключ `smsManagement` во все 4 словаря (ru, en, fr, ar)
- [x] Добавить `smsRuSettings` в `fr.json` (отсутствовал)
- [x] Заменить хардкоженные строки в `SMSRuSettings` на `dictionary.*` (42 новых ключа во всех 4 словарях)
- [x] `loadSettings` обёрнут в `useCallback` с `[dictionary]` dep

**Критерии завершения:**

- [x] Нет хардкоженных строк на русском в компоненте
- [x] `pnpm lint` не выдаёт `react-hooks/exhaustive-deps` для этого компонента
- [x] UI корректно отображается при переключении языка

**Оценка времени:** 45 минут

---

## 📈 Прогресс

- **Выполнено:** 100%
- **Текущий этап:** Завершён

**Обновление:** 2026-03-16 — Все этапы выполнены. Дополнительно: удалён экспериментальный функционал «Использовать бесплатные SMS первыми» (`useFreeFirst`) — SMS.ru API не предоставляет параметра для явного выбора бесплатной квоты, управление происходит автоматически на стороне сервиса.

---

## ⚠️ Риски и митигация

1. **Риск: `smsManagement` отсутствует в seed/миграциях ролей**
   - Вероятность: Средняя
   - Влияние: Высокое — пользователи с правильными ролями не смогут войти в настройки
   - Митигация: Проверить как добавляются права в других модулях (например `smtpManagement`); обновить seed

2. **Риск: Метрики-файл конфликтует с существующей структурой**
   - Вероятность: Низкая
   - Влияние: Среднее
   - Митигация: Изучить `src/lib/metrics/` перед добавлением

---

## 🧪 Тестирование

### План тестирования:

- [ ] Ручное: проверить GET баланса в UI (уже работает после фикса)
- [ ] Ручное: проверить тестовую отправку SMS
- [ ] Ручное: проверить что права `smtpManagement` не дают доступ к SMS-настройкам
- [ ] Ручное: проверить audit event в `/admin/events` после сохранения настроек
- [ ] Ручное: проверить метрики на `/api/metrics`

---

## 📚 Документация

### Что нужно задокументировать:

- [ ] Обновить анализ: отметить исправленные проблемы
- [ ] Обновить `STATUS_INDEX.md` при завершении

---

## ✅ Чек-лист завершения

- [ ] Этап 1 выполнен (gitignore)
- [ ] Этап 2 выполнен (permissions)
- [ ] Этап 3 выполнен (audit events)
- [ ] Этап 4 выполнен (метрики)
- [ ] Этап 5 выполнен (типизация)
- [ ] Этап 6 выполнен (i18n)
- [ ] Ручное тестирование пройдено
- [ ] `STATUS_INDEX.md` обновлён
