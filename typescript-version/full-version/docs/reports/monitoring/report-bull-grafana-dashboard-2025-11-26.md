# Отчёт: Grafana Dashboard для Bull/Notifications Queue

**Дата:** 2025-11-26  
**Статус:** ✅ Завершён

---

## 📋 Связанные документы

- [Анализ](../../analysis/monitoring/analysis-bull-grafana-dashboard-2025-11-26.md)
- [План](../../plans/active/plan-bull-grafana-dashboard-2025-11-26.md)

---

## 🎯 Цель

Создать Grafana Dashboard для визуализации метрик Bull очередей (уведомления) с полной интеграцией в существующую систему мониторинга.

---

## ✅ Выполненные задачи

### 1. Создание Grafana Dashboard

**Файл:** `monitoring/grafana/dashboards/notifications-dashboard.json`

**Панели (13 штук):**

| # | Панель | Тип | PromQL |
|---|--------|-----|--------|
| 1 | Queue Size | stat | `sum(notification_queue_size)` |
| 2 | Jobs Added (5m) | stat | `sum(increase(notification_jobs_added_total[5m]))` |
| 3 | Jobs Processed (5m) | stat | `sum(increase(notification_jobs_processed_total[5m]))` |
| 4 | Failures (5m) | stat | `sum(increase(notifications_failed_total[5m]))` |
| 5 | Retries (5m) | stat | `sum(increase(notification_retries_total[5m]))` |
| 6 | Queue Type | stat | `topk(1, notification_queue_size) by (queue_type)` |
| 7 | Queue Size Over Time | timeseries | `notification_queue_size` |
| 8 | Queue Switches | timeseries | `increase(notification_queue_switches_total[5m])` |
| 9 | Sent by Channel | timeseries | `sum(rate(notifications_sent_total[5m])) by (channel)` |
| 10 | Failed by Channel | timeseries | `sum(rate(notifications_failed_total[5m])) by (channel)` |
| 11 | Send Duration (p50/p95/p99) | timeseries | `histogram_quantile(...)` |
| 12 | Jobs Throughput | timeseries | `sum(rate(notification_jobs_processed_total[1m])) * 60` |
| 13 | Retries by Attempt | timeseries | `sum(rate(notification_retries_total[5m])) by (attempt)` |
| 14 | Error Types Distribution | piechart | `sum(notifications_failed_total) by (error_type)` |
| 15 | Scenario Executions | timeseries | `sum(rate(scenario_executions_total[5m])) by (status)` |

**Фичи:**

- ✅ Переменная `environment` для фильтрации (All/development/production)
- ✅ Авто-обновление каждые 10 секунд
- ✅ Цветовая индикация пороговых значений (зелёный/жёлтый/красный)
- ✅ Группировка панелей по Row (6 рядов)
- ✅ Тёмная тема совместима

### 2. Grafana Provisioning

**Статус:** Уже настроен

Файл `monitoring/grafana/provisioning/dashboards/dashboards.yml` сканирует `/etc/grafana/dashboards`, куда монтируется `./grafana/dashboards`. Новый дашборд подхватится автоматически.

### 3. Документация

**Обновлённые файлы:**

- `docs/ROOT_FILES_DESCRIPTION.md` — добавлена секция Grafana Dashboard
- `docs/STATUS_INDEX.md` — добавлен анализ
- `docs/analysis/monitoring/analysis-bull-grafana-dashboard-2025-11-26.md` — создан
- `docs/plans/active/plan-bull-grafana-dashboard-2025-11-26.md` — создан
- `docs/reports/monitoring/report-bull-grafana-dashboard-2025-11-26.md` — создан (этот файл)

---

## 🖼️ Структура дашборда

```
┌─────────────────────────────────────────────────────────────────────┐
│  📊 Overview Stats                                                   │
│  ┌──────┬──────┬──────┬──────┬──────┬──────┐                        │
│  │Queue │Jobs  │Jobs  │Fail  │Retry │Queue │                        │
│  │Size  │Added │Proc  │(5m)  │(5m)  │Type  │                        │
│  └──────┴──────┴──────┴──────┴──────┴──────┘                        │
├─────────────────────────────────────────────────────────────────────┤
│  📈 Queue Activity                                                   │
│  ┌──────────────────────┬──────────────────────┐                    │
│  │ Queue Size Over Time │ Queue Switches       │                    │
│  └──────────────────────┴──────────────────────┘                    │
├─────────────────────────────────────────────────────────────────────┤
│  📧 Notifications by Channel                                         │
│  ┌──────────────────────┬──────────────────────┐                    │
│  │ Sent by Channel      │ Failed by Channel    │                    │
│  │ (email/sms/browser)  │ (email/sms/browser)  │                    │
│  └──────────────────────┴──────────────────────┘                    │
├─────────────────────────────────────────────────────────────────────┤
│  ⚡ Performance                                                       │
│  ┌──────────────────────┬──────────────────────┐                    │
│  │ Duration p50/95/99   │ Jobs Throughput      │                    │
│  └──────────────────────┴──────────────────────┘                    │
├─────────────────────────────────────────────────────────────────────┤
│  🔄 Retries & Errors                                                 │
│  ┌──────────────────────┬──────────────────────┐                    │
│  │ Retries by Attempt   │ Error Types (Pie)    │                    │
│  └──────────────────────┴──────────────────────┘                    │
├─────────────────────────────────────────────────────────────────────┤
│  📋 Scenarios                                                        │
│  ┌──────────────────────────────────────────────┐                   │
│  │ Scenario Executions                          │                   │
│  └──────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Как использовать

### 1. Запустить мониторинг

```bash
pnpm dev:with-socket:monitoring:with-redis:with-bull
```

Или только мониторинг:

```bash
cd monitoring && docker compose up -d
```

### 2. Открыть Grafana

URL: http://localhost:9091

**Credentials:** admin / admin (при первом входе)

### 3. Найти дашборд

1. Кликнуть на **Dashboards** (иконка 4 квадрата слева)
2. Найти **"Materio Notifications & Queue Overview"**
3. Открыть

### 4. Проверить данные

Данные появятся после:

- Запуска приложения (`pnpm dev`)
- Отправки уведомлений (любой канал)
- Скрапинга Prometheus (каждые 15 секунд)

---

## 📊 Метрики, используемые в дашборде

| Метрика | Источник |
|---------|----------|
| `notification_queue_size` | `src/lib/metrics/notifications.ts` |
| `notification_jobs_added_total` | `src/lib/metrics/notifications.ts` |
| `notification_jobs_processed_total` | `src/lib/metrics/notifications.ts` |
| `notifications_sent_total` | `src/lib/metrics/notifications.ts` |
| `notifications_failed_total` | `src/lib/metrics/notifications.ts` |
| `notification_send_duration_seconds` | `src/lib/metrics/notifications.ts` |
| `notification_queue_switches_total` | `src/lib/metrics/notifications.ts` |
| `notification_retries_total` | `src/lib/metrics/notifications.ts` |
| `scenario_executions_total` | `src/lib/metrics/notifications.ts` |

---

## ⏱️ Затраченное время

| Этап | План | Факт |
|------|------|------|
| Создание Dashboard JSON | 30 мин | 15 мин |
| Проверка Provisioning | 5 мин | 2 мин |
| Документация | 15 мин | 15 мин |
| **Итого** | 50 мин | **~32 мин** |

---

## ✅ Чек-лист завершения

- [x] Дашборд создан (`notifications-dashboard.json`)
- [x] JSON валиден (13 панелей, 6 рядов)
- [x] Provisioning настроен (auto-load)
- [x] ROOT_FILES_DESCRIPTION.md обновлён
- [x] STATUS_INDEX.md обновлён
- [x] Анализ создан
- [x] План создан
- [x] Отчёт создан

---

## 📈 Итоги

Создан полноценный Grafana Dashboard для мониторинга Bull очередей (уведомления):

- **15 панелей** с визуализацией всех доступных метрик
- **6 логических групп** (Overview, Activity, Channels, Performance, Retries, Scenarios)
- **Фильтрация по environment** (development/production)
- **Auto-refresh** каждые 10 секунд
- **Цветовая индикация** пороговых значений

Dashboard интегрируется с существующей системой мониторинга и загружается автоматически при старте Grafana.

