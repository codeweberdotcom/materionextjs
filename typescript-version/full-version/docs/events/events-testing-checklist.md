# Чек-лист проверки Events в реальном приложении

**Дата:** 2025-01-22

---

## ✅ Подготовка

- [ ] Приложение запущено (`npm run dev`)
- [ ] База данных доступна
- [ ] Пользователь авторизован (администратор)

---

## 📤 Тест 1 - Экспорт пользователей

### Шаги:

1. Откройте `http://localhost:3000/ru/apps/user/list`
2. Нажмите кнопку **"Экспорт"**
3. Выберите формат (XLSX или CSV)
4. Дождитесь завершения экспорта

### Проверка:

```bash
npm run check:events export 5
```

### Ожидаемый результат:

- ✅ Событие `export.started` с `severity: 'info'`
- ✅ Событие `export.completed` с `severity: 'info'`
- ✅ Оба события имеют одинаковый `correlationId`
- ✅ В `actor` указан ID текущего пользователя
- ✅ В `payload` есть `entityType: 'users'`, `format`, `recordCount`

---

## 📥 Тест 2 - Импорт пользователей

### Шаги:

1. На странице пользователей нажмите **"Импорт"**
2. Загрузите файл (CSV или XLSX)
3. Дождитесь предпросмотра
4. Нажмите **"Импортировать"**

### Проверка:

```bash
npm run check:events import 10
```

### Ожидаемый результат:

- ✅ Событие `import.preview` (при предпросмотре)
- ✅ Событие `import.started` с `severity: 'info'`
- ✅ Событие `import.completed` с `severity: 'info'`
- ✅ Все события имеют одинаковый `correlationId`
- ✅ В `payload` есть `fileName`, `fileSize`, `mode`, `successCount`

---

## 🗑️ Тест 3 - Массовое удаление

### Шаги:

1. Выберите несколько пользователей (чекбоксы)
2. Нажмите кнопку **"Удалить"**
3. Подтвердите удаление

### Проверка:

```bash
npm run check:events user_management 10
```

### Ожидаемый результат:

- ✅ Событие `user_management.bulk_delete` с `severity: 'warning'`
- ✅ Событие `user_management.bulk_delete_success` с `severity: 'info'`
- ✅ Оба события имеют одинаковый `correlationId`
- ✅ В `payload` есть `userIds`, `count`, `successCount`, `mode: 'delete'`

---

## ✅ Тест 4 - Массовая активация

### Шаги:

1. Выберите несколько пользователей
2. Нажмите кнопку **"Активировать"**

### Проверка:

```bash
npm run check:events user_management 10
```

### Ожидаемый результат:

- ✅ Событие `user_management.bulk_activate` с `severity: 'info'`
- ✅ Событие `user_management.bulk_status_change_success` с `severity: 'info'`
- ✅ В `payload` есть `action: 'activate'`

---

## ❌ Тест 5 - Массовая деактивация

### Шаги:

1. Выберите несколько пользователей
2. Нажмите кнопку **"Деактивировать"**

### Проверка:

```bash
npm run check:events user_management 10
```

### Ожидаемый результат:

- ✅ Событие `user_management.bulk_deactivate` с `severity: 'info'`
- ✅ Событие `user_management.bulk_status_change_success` с `severity: 'info'`
- ✅ В `payload` есть `action: 'deactivate'`

---

## 🔍 Дополнительные проверки

### Проверка correlationId

```bash
# Найти все события с определенным correlationId
npm run check:events | grep "correlation-id"
```

**Ожидаемый результат:**

- События одной операции (started → completed) имеют одинаковый `correlationId`

### Проверка actorId

```bash
npm run check:events | grep "Инициатор"
```

**Ожидаемый результат:**

- Большинство событий имеют `actorId` (ID пользователя)
- События без `actorId` только если пользователь не авторизован

### Проверка через Prisma Studio

```bash
npx prisma studio
```

Откройте таблицу `Event` и проверьте:

- Записи создаются
- Поля заполнены корректно
- `correlationId` связывает события
- `actorId` указан

---

## 📊 Итоговая проверка

После выполнения всех тестов:

```bash
# Все события экспорта
npm run check:events export

# Все события импорта
npm run check:events import

# Все массовые операции
npm run check:events user_management

# Все события (общая статистика)
npm run check:events
```

---

## ❌ Устранение проблем

### События не появляются

1. **Проверьте логи:**
   - Консоль браузера (F12)
   - Консоль сервера (`npm run dev`)

2. **Проверьте базу данных:**

   ```bash
   npx prisma studio
   ```

3. **Проверьте авторизацию:**
   - Убедитесь, что вы вошли в систему
   - Проверьте cookies

### actorId всегда null

- Убедитесь, что `useAuth()` работает
- Проверьте, что пользователь авторизован
- Проверьте cookies в браузере

### correlationId не совпадает

- Это нормально, если выполнили несколько операций
- Проверьте, что события одной операции связаны

---

**Подробная инструкция:** `docs/events-verification-guide.md`  
**Быстрая проверка:** `docs/events-quick-check.md`
