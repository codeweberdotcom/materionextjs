# Руководство по проверке работоспособности Events

**Дата:** 2025-01-22  
**Версия:** 1.0

---

## Быстрая проверка

### 1. Проверка событий в базе данных

Используйте скрипт для проверки записанных событий:

```bash
# Проверить все события (последние 20)
npm run check:events

# Проверить события экспорта
npm run check:events export 10

# Проверить события импорта
npm run check:events import 10

# Проверить события массовых операций
npm run check:events user_management 10
```

### 2. Проверка через API

Откройте в браузере или используйте curl:

```bash
# Получить все события экспорта
curl "http://localhost:3000/api/admin/events?source=export&limit=10" \
  -H "Cookie: your-session-cookie"

# Получить все события импорта
curl "http://localhost:3000/api/admin/events?source=import&limit=10" \
  -H "Cookie: your-session-cookie"

# Получить события массовых операций
curl "http://localhost:3000/api/admin/events?source=user_management&limit=10" \
  -H "Cookie: your-session-cookie"
```

### 3. Проверка через UI

1. Откройте приложение: `http://localhost:3000`
2. Войдите как администратор
3. Перейдите в раздел **Admin → Events** (если доступен)
4. Отфильтруйте по источнику:
   - `export` - события экспорта
   - `import` - события импорта
   - `user_management` - массовые операции

---

## Пошаговая проверка

### Шаг 1: Запуск приложения

```bash
npm run dev
```

Убедитесь, что приложение запустилось без ошибок.

### Шаг 2: Проверка экспорта

1. Откройте страницу пользователей: `http://localhost:3000/ru/apps/user/list`
2. Нажмите кнопку **"Экспорт"**
3. Выберите формат (XLSX или CSV)
4. Дождитесь завершения экспорта

**Проверка:**
```bash
npm run check:events export 5
```

**Ожидаемый результат:**
- Должны быть события `export.started` и `export.completed`
- Оба события должны иметь одинаковый `correlationId`
- В `actor` должен быть указан ID текущего пользователя
- В `payload` должны быть данные о формате и количестве записей

### Шаг 3: Проверка импорта

1. На странице пользователей нажмите кнопку **"Импорт"**
2. Выберите файл для импорта (CSV или XLSX)
3. Дождитесь предпросмотра
4. Нажмите **"Импортировать"**

**Проверка:**
```bash
npm run check:events import 10
```

**Ожидаемый результат:**
- Должно быть событие `import.preview` (при предпросмотре)
- Должны быть события `import.started` и `import.completed`
- Все события должны иметь одинаковый `correlationId`
- В `payload` должны быть данные о файле и результатах

### Шаг 4: Проверка массовых операций

1. На странице пользователей выберите несколько пользователей (чекбоксы)
2. Нажмите кнопку **"Удалить"**, **"Активировать"** или **"Деактивировать"**
3. Подтвердите операцию

**Проверка:**
```bash
npm run check:events user_management 10
```

**Ожидаемый результат:**
- Должно быть событие начала операции (`bulk_delete`, `bulk_activate`, `bulk_deactivate`)
- Должно быть событие результата (`bulk_delete_success`, `bulk_status_change_success` или `bulk_*_failed`)
- Все события должны иметь одинаковый `correlationId`
- В `payload` должны быть `userIds` и количество операций

---

## Проверка correlationId

События одной операции должны быть связаны через `correlationId`:

```bash
# Найти все события с определенным correlationId
npm run check:events | grep "correlation-id-123"
```

Или через SQL (если используете Prisma Studio):

```sql
SELECT * FROM Event 
WHERE correlationId = 'your-correlation-id' 
ORDER BY createdAt;
```

---

## Проверка actorId (инициатора)

Все события должны содержать информацию об инициаторе:

```bash
npm run check:events | grep "Инициатор"
```

**Ожидаемый результат:**
- Большинство событий должны иметь `actorId` (ID пользователя)
- События без `actorId` должны быть только в случае, если пользователь не авторизован

---

## Проверка структуры событий

### Экспорт

Проверьте, что события экспорта содержат:

```json
{
  "source": "export",
  "module": "users",
  "type": "export.started" | "export.completed" | "export.failed",
  "severity": "info" | "error",
  "actor": {
    "type": "user",
    "id": "user-id"
  },
  "payload": {
    "entityType": "users",
    "format": "xlsx" | "csv",
    "recordCount": 123,
    "hasFilters": false,
    "hasSelectedIds": false
  },
  "correlationId": "uuid"
}
```

### Импорт

Проверьте, что события импорта содержат:

```json
{
  "source": "import",
  "module": "users",
  "type": "import.started" | "import.completed" | "import.failed" | "import.preview",
  "severity": "info" | "warning" | "error",
  "actor": {
    "type": "user",
    "id": "user-id"
  },
  "payload": {
    "entityType": "users",
    "fileName": "users.csv",
    "fileSize": 12345,
    "mode": "create" | "update" | "upsert",
    "successCount": 10,
    "errorCount": 2,
    "totalProcessed": 12
  },
  "correlationId": "uuid"
}
```

### Массовые операции

Проверьте, что события массовых операций содержат:

```json
{
  "source": "user_management",
  "module": "users",
  "type": "user_management.bulk_delete" | "user_management.bulk_activate" | ...,
  "severity": "info" | "warning" | "error",
  "actor": {
    "type": "user",
    "id": "user-id"
  },
  "payload": {
    "userIds": ["1", "2", "3"],
    "count": 3,
    "successCount": 3,
    "failedCount": 0,
    "mode": "delete" | "anonymize",
    "action": "activate" | "deactivate"
  },
  "correlationId": "uuid"
}
```

---

## Устранение проблем

### Проблема: События не записываются

**Проверьте:**
1. Приложение запущено и работает
2. База данных доступна
3. Нет ошибок в консоли браузера
4. Нет ошибок в логах сервера

**Решение:**
```bash
# Проверьте логи приложения
npm run dev

# Проверьте подключение к БД
npx prisma studio
```

### Проблема: actorId всегда null

**Причина:** Пользователь не авторизован или `useAuth()` не работает

**Решение:**
1. Убедитесь, что вы вошли в систему
2. Проверьте, что `AuthProvider` правильно настроен
3. Проверьте cookies в браузере

### Проблема: correlationId не совпадает

**Причина:** События генерируются в разных операциях

**Решение:**
- Это нормально, если вы выполнили несколько операций
- Проверьте, что события одной операции (started → completed) имеют одинаковый `correlationId`

---

## Примеры проверки

### Проверка последних событий экспорта

```bash
npm run check:events export 5
```

**Ожидаемый вывод:**
```
🔍 Проверка событий в базе данных...

✅ Найдено событий: 5

📊 Статистика по источникам:
  export: 5 событий
    - export.started: 3
    - export.completed: 3
    - export.failed: 0

📋 Последние события:
1. [INFO] export.export.completed
   Сообщение: Export completed: 15 records
   Инициатор: user user-123
   Correlation ID: abc-123-def
   Время: 2025-01-22T12:00:00.000Z
   Сущность: users
   Формат: xlsx
   Записей: 15
```

### Проверка связанных событий

Скрипт автоматически показывает события, связанные через `correlationId`:

```
📎 Примеры связанных событий (по correlationId):

  Correlation ID: abc-123-def
    - export.started (info)
    - export.completed (info)
```

---

## Дополнительные проверки

### Проверка через Prisma Studio

```bash
npx prisma studio
```

Откройте таблицу `Event` и проверьте:
- Записи создаются
- Поля заполнены корректно
- `correlationId` связывает события
- `actorId` указан для авторизованных пользователей

### Проверка через SQL

Если используете SQLite или PostgreSQL:

```sql
-- Последние события экспорта
SELECT * FROM Event 
WHERE source = 'export' 
ORDER BY createdAt DESC 
LIMIT 10;

-- События с определенным correlationId
SELECT * FROM Event 
WHERE correlationId = 'your-correlation-id'
ORDER BY createdAt;

-- Статистика по типам событий
SELECT type, COUNT(*) as count 
FROM Event 
WHERE source = 'export' 
GROUP BY type;
```

---

## Чек-лист проверки

- [ ] Приложение запускается без ошибок
- [ ] Экспорт создает события `export.started` и `export.completed`
- [ ] Импорт создает события `import.started`, `import.completed` и `import.preview`
- [ ] Массовые операции создают соответствующие события
- [ ] Все события имеют `correlationId`
- [ ] События одной операции связаны через `correlationId`
- [ ] Большинство событий имеют `actorId` (инициатор)
- [ ] `payload` содержит корректные данные
- [ ] Ошибки записываются как события с `severity: 'error'`

---

**Последнее обновление:** 2025-01-22  
**Автор:** AI Assistant

