# Анализ модуля «User»

**Дата проведения:** 2025-11-24  
**Статус:** Завершен  
**Приоритет:** Высокий

---

## 🎯 Цель анализа

Оценить текущее состояние пользовательского модуля (создание, управление, профиль, импорт/экспорт) и выявить проблемные области, влияющие на безопасность, работоспособность и поддерживаемость.

---

## 📊 Текущее состояние

### Что анализируется:
- REST API для админских операций над пользователями (`src/app/api/admin/users/*`)
- Публичные и пользовательские API (`src/app/api/users`, `src/app/api/user/*`)
- Сервисный слой (`src/services/business/userService.ts`, `src/services/database/userRepository.ts`)
- UI модули списка пользователей (`src/views/apps/user/list/*`) и их серверные экшены (`src/app/[lang]/(dashboard)/(private)/apps/user/list/page.tsx`, `src/app/server/actions.ts`)
- Импорт/экспорт через `UserAdapter` и связанные unit-тесты
- Наличие тестового покрытия (`tests/unit/**`)
- Документация `docs/api/users.md`

### Методология:
- Просмотр исходного кода и структуры директорий
- Сопоставление реализации с документацией и требованиями AI Workflow
- Анализ unit-тестов и выявление отсутствующих сценариев
- Проверка кэширования, сервисных и файловых зависимостей

---

## 🔍 Результаты анализа

### Архитектура модуля
Модуль построен по классической схеме: Prisma-репозиторий → сервис → API → UI. Админские маршруты используют in-memory кэш на 30 секунд и ручную очистку через `clearCache`. UI таблица загружает стартовые данные через server action и далее взаимодействует с REST API для CRUD, массовых операций и интеграции с `eventService`. Экспорт/импорт реализован отдельным адаптером, который напрямую использует REST API.

#### Потоки данных
- **Авторизация**: `requireAuth()` → проверка роли через `checkPermission()` → доступ/redirect.
- **Чтение пользователей**: `GET /api/admin/users` → кеш → Prisma → трансформация к `UsersType` → UI таблица → контекст присутствия/чат.
- **Создание/обновление**: UI формы → REST (FormData/JSON) → Prisma → очистка кеша (не везде) → повторный fetch.
- **Импорт/экспорт**: `UserAdapter` → `fetch /api/admin/users` → CSV → `eventService` логирует массовые операции.
- **Публичный список**: `GET /api/users` → `userService` → чат/контакты.
- **Профиль**: `/api/user/profile`, `/api/user/change-password`, `/api/user/avatar` → direct Prisma операции.

#### Основные зависимости
- **База**: Prisma + SQLite/движок из `prisma/schema.prisma`.
- **Хранилище файлов**: локальная файловая система (`public/images/avatars`, `public/uploads/avatars`).
- **Сервисы**: `emailService`, `eventService`, `PresenceProvider`, `useUnreadByContact`.
- **UI**: MUI + `@tanstack/react-table` + Toastify.
- **Тесты**: Vitest (unit), пока нет integration/e2e для user-модуля.

### Найденные проблемы:

1. **Пароль новых пользователей сохраняется в открытом виде**
   - **Описание:** При создании пользователя через `/api/admin/users` поле `password` захардкожено как `'password123'` и не хэшируется, что нарушает требования безопасности и делает вход невозможным (маршрут логина проверяет bcrypt-хэш).
   - **Влияние:** Высокое (уязвимость + невозможность авторизации созданных аккаунтов)
   - **Приоритет:** Критический
   - **Местоположение:** `src/app/api/admin/users/route.ts` (блок создания пользователя)

2. **Тоггл статуса одного пользователя всегда падает**
   - **Описание:** UI вызывает `fetch('/api/admin/users/:id', { method: 'PATCH' })` без тела, тогда как API требует JSON с `isActive`. В результате `PATCH` бросает ошибку парсинга тела и статус не меняется.
   - **Влияние:** Среднее (невозможно включить/выключить пользователя из таблицы)
   - **Приоритет:** Высокий
   - **Местоположение:** 
     - `src/views/apps/user/list/UserListTable.tsx` (`handleToggleUserStatus`)
     - `src/app/api/admin/users/[id]/route.ts` (обязательное чтение `request.json()` и проверка `isActive`)

3. **Импорт пользователей запрашивает несуществующие endpoints**
   - **Описание:** `UserAdapter.saveImportedData` использует URL `/api/admin/users/update-by-email` и `/api/admin/users/upsert`, которых нет в проекте. Любые импорт-операции в режимах update/upsert завершаются 404, а ошибки копятся в отчёте импорта.
   - **Влияние:** Среднее (функционал импорта с обновлением фактически не работает)
   - **Приоритет:** Высокий
   - **Местоположение:** `src/services/adapters/UserAdapter.ts` (режимы update/upsert)

4. **Несогласованное хранение аватаров**
   - **Описание:** Админский `POST /api/admin/users` пишет файл в `public/images/avatars`, тогда как `/api/user/avatar` сохраняет base64 в колонке `image`. В итоге часть пользователей хранит бинарные данные в БД, часть — относительные пути, что усложняет CDN/кеш, увеличивает БД и ломает миграцию файлов.
   - **Влияние:** Среднее (рост БД, сложности кэша, непредсказуемое отображение)
   - **Приоритет:** Средний
   - **Местоположение:** 
     - `src/app/api/admin/users/route.ts` (запись файла в `public/images/avatars`)
     - `src/app/api/user/avatar/route.ts` (сохранение base64 строки)

5. **Кэш списка пользователей не очищается при PATCH**
   - **Описание:** In-memory кэш очищается при `POST`, `PUT` и `DELETE`, но не при `PATCH` (смена `isActive`). UI после переключения статуса перезапрашивает `/api/admin/users`, но пока не истечёт 30 секунд, API отдаёт устаревший список.
   - **Влияние:** Низкое/Среднее (статус в UI «отскакивает», возникают гонки в массовых операциях)
   - **Приоритет:** Средний
   - **Местоположение:** `src/app/api/admin/users/route.ts` (кэш) и `src/app/api/admin/users/[id]/route.ts` (отсутствие очистки в `PATCH`)

#### Дополнительные наблюдения
- Нет централизованного DTO/валидации тел запросов; ручное чтение FormData/JSON повышает риск расхождений.
- `UserService` и REST-роуты используют разные стратегии логирования/обработки ошибок (где-то logger, где-то `console.error`).
- Массовые операции сильно зависят от фронтенда (нет серверной транзакции/батчей).

### Найденные возможности:

1. **Единый сервисный слой** — админские маршруты дублируют логику `userService`. Вынесение в сервис позволит переиспользовать в API/адаптерах и покрыть unit-тестами.
2. **API-пагинация и фильтры** — сейчас `/api/admin/users` всегда отдаёт весь список. Добавление параметров фильтрации (которые уже готовит `UserAdapter`) снизит нагрузку и упростит проверку дубликатов.
3. **Интеграционные тесты** — существующие тесты покрывают только адаптер и bulk-операции на UI. Нет e2e/unit тестов для критичных маршрутов (`POST/PUT/PATCH /api/admin/users`, `PUT /api/user/profile`).

---

## 💡 Рекомендации

1. **Хеширование пароля при создании пользователя**
   - Использовать `bcrypt.hash` (или вынести в `userService.createUser`) и убрать захардкоженный пароль. Добавить генерацию временного пароля/сброс по e-mail.
   - Сложность: Средняя · Время: 2-3 часа.

2. **Исправить PATCH запрос и валидировать payload**
   - На фронте: отправлять `JSON.stringify({ isActive: !currentValue })`.
   - На бэке: добавить защиту от пустого тела и дружелюбные ошибки.
   - Дополнительно: очистка кэша после успешного PATCH.
   - Сложность: Средняя · Время: 2 часа.

3. **Реализовать недостающие endpoints для импорта или ограничить режимы**
   - Либо добавить `/api/admin/users/update-by-email` и `/upsert`, либо убрать их из адаптера и явно сообщать пользователю, что доступен только `create`.
   - Сложность: Средняя/Высокая (если реализовывать API) · Время: 1-2 дня.

4. **Унифицировать хранение аватаров**
   - Выбрать единый подход (файлы в `/uploads/avatars` или объектное хранилище) и мигрировать данные; убрать сохранение base64 в БД.
   - Сложность: Средняя · Время: 1 день (без миграции исторических файлов).

5. **Расширить тестовое покрытие**
   - Добавить unit/integ тесты для `/api/admin/users` CRUD и `/api/user/*`.
   - Автоматизировать сценарии импорта (успех/ошибка/дубликаты).
   - Сложность: Средняя · Время: 1-2 дня.

6. **Ввести единые DTO/валидаторы**
   - Использовать Zod/Valibot для описания payload’ов, чтобы исключить дублирование при чтении `FormData` и JSON.
   - Сложность: Средняя · Время: 1 день.

---

## 📝 Выводы

Модуль предоставляет полный набор функций, но содержит критические дефекты (нехэшированные пароли, неработающий toggle, сломанный импорт), которые блокируют эксплуатацию. Требуется привести API и UI в согласованное состояние, унифицировать хранение бинарных данных и закрыть пробелы в тестах.

---

## 🔗 Связанные документы

- `docs/api/users.md` — описание целевых API
- `docs/plans/active/plan-e2e-test-improvements-user-creation-2025-01-24.md` — план улучшения покрытий
- `docs/user-operations/*.md` — регламенты массовых операций

