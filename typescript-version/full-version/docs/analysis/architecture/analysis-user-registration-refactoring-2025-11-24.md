# Анализ: Рефакторинг регистрации пользователей для доски объявлений

**Дата проведения:** 2025-11-24  
**Статус:** Завершен  
**Приоритет:** Высокий

---

## 🎯 Цель анализа

Проанализировать текущую систему регистрации и определить необходимые изменения для поддержки:
1. **Настраиваемых режимов регистрации** (выбор в админке):
   - Регистрация по email ИЛИ телефону (один из вариантов)
   - Регистрация по email И телефону одновременно (оба обязательны)
2. Подтверждения email (обязательное при регистрации по email)
3. Верификации телефона через SMS (обязательная для полного доступа)
4. Ограничения функционала до верификации телефона (даже при подтвержденном email)
5. Интеграции с SMS.ru для отправки SMS

---

## 📊 Текущее состояние

### Схема User (Prisma)

```prisma
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  password      String
  emailVerified DateTime?  // ❌ Не используется при регистрации
  image         String?
  roleId        String
  language      String    @default("Russian")
  currency      String    @default("RUB")
  country       String    @default("russia")
  isActive      Boolean   @default(true)
  lastSeen      DateTime?
  // ... relations
}
```

### Текущий процесс регистрации

**Endpoint:** `POST /api/register`

**Принимаемые данные:**
- `name` (опционально)
- `email` (обязательно)
- `password` (обязательно)

**Что сохраняется:**
- `name`, `email`, `password` (хэш)
- `roleId` (роль "user")
- `country: 'russia'`, `language: 'Russian'`, `currency: 'RUB'`
- `isActive: true`
- `emailVerified: null` ❌ (не устанавливается)

**Проблемы:**
1. ❌ Email не подтверждается при регистрации
2. ❌ Нет поддержки регистрации по телефону
3. ❌ Нет верификации телефона
4. ❌ Поле `emailVerified` существует, но не используется
5. ❌ Нет поля для номера телефона
6. ❌ Нет поля для статуса верификации телефона

---

## 🔍 Требования для доски объявлений

### 1. Настраиваемые режимы регистрации

**Требования:**
- Выбор режима регистрации в админке (настройки продакшена):
  - `email_or_phone` - регистрация по email ИЛИ телефону (один из вариантов)
  - `email_and_phone` - регистрация по email И телефону одновременно (оба обязательны)
- Настройки сохраняются и применяются при регистрации
- Возможность изменения режима администратором

**Текущее состояние:**
- ❌ Нет системы настроек регистрации
- ❌ Нет UI для настройки режимов
- ❌ Нет API для управления настройками

### 2. Подтверждение Email (обязательное при регистрации по email)

**Требования:**
- Пользователь должен подтвердить email перед доступом в админку
- Отправка письма с токеном подтверждения
- Ссылка для подтверждения
- После подтверждения email: пользователь может видеть админку, но не может управлять

**Текущее состояние:**
- ✅ Поле `emailVerified` существует в схеме
- ✅ Модель `VerificationToken` существует
- ❌ Не используется при регистрации
- ❌ Нет endpoint для подтверждения email
- ❌ Нет отправки письма с токеном

### 3. Регистрация по номеру телефона

**Требования:**
- Альтернатива или дополнение к регистрации по email (в зависимости от режима)
- Валидация формата номера телефона (российские и международные форматы)
- Уникальность номера телефона
- Нормализация формата (+79991234567)

**Текущее состояние:**
- ❌ Нет поля `phone` в схеме User
- ❌ Нет поля `phoneVerified` в схеме User
- ❌ Нет валидации номера телефона
- ❌ Нет endpoint для регистрации по телефону

### 4. Верификация телефона через SMS.ru (обязательная для полного доступа)

**Требования:**
- Отправка SMS с кодом подтверждения через SMS.ru
- Валидация кода подтверждения (6-значный числовой код)
- Ограничение попыток ввода кода (3 попытки)
- Rate limiting для отправки SMS (1 SMS в минуту на номер)
- Обязательная верификация телефона для полного доступа (даже при подтвержденном email)

**Текущее состояние:**
- ❌ Нет интеграции с SMS.ru
- ❌ Нет хранения кодов подтверждения
- ❌ Нет endpoint для отправки/проверки SMS кода

### 5. Ограничение функционала до верификации

**Требования:**
- Система уровней доступа:
  - `NONE` - нет верификации
  - `EMAIL_ONLY` - верифицирован email (может видеть админку, но не управлять)
  - `PHONE_ONLY` - верифицирован телефон (редкий случай)
  - `FULL` - верифицированы email и телефон (полный доступ)
- Просмотр админки: требует `EMAIL_ONLY` или выше
- Управление (создание объявлений, отправка сообщений): требует `FULL`

**Текущее состояние:**
- ❌ Нет системы уровней доступа
- ❌ Нет проверок верификации в endpoints
- ❌ Нет ограничений функционала

---

## 💡 Рекомендации по рефакторингу

### Этап 1: Расширение схемы User

**Изменения в Prisma schema:**

```prisma
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?    @unique  // ✅ Сделать опциональным
  phone         String?   @unique  // ✅ Добавить поле телефона
  password      String
  emailVerified DateTime?           // ✅ Использовать для email
  phoneVerified DateTime?            // ✅ Добавить для телефона
  image         String?
  roleId        String
  language      String    @default("Russian")
  currency      String    @default("RUB")
  country       String    @default("russia")
  isActive      Boolean   @default(true)
  lastSeen      DateTime?
  // ... relations
  
  @@index([email])
  @@index([phone])
  // ✅ Обеспечить, что хотя бы email или phone заполнен
}
```

**Миграция:**
- Сделать `email` опциональным (может быть null)
- Добавить поле `phone` (String?, unique)
- Добавить поле `phoneVerified` (DateTime?)
- Добавить индексы для поиска

### Этап 2: Модель для кодов верификации

**Новая модель в Prisma:**

```prisma
model VerificationCode {
  id          String   @id @default(cuid())
  identifier  String   // email или phone
  code        String   // 6-значный код
  type        String   // 'email' | 'phone'
  expires     DateTime
  attempts    Int      @default(0)
  maxAttempts Int      @default(3)
  verified    Boolean  @default(false)
  createdAt   DateTime @default(now())
  
  @@index([identifier, type])
  @@index([code])
}
```

### Этап 3: Настройки регистрации в админке

**Создать систему настроек:**

- Модель `RegistrationSettings` (или JSON файл):
  - `registrationMode`: `'email_or_phone' | 'email_and_phone'`
  - `requirePhoneVerification`: `Boolean` (всегда true)
  - `requireEmailVerification`: `Boolean` (всегда true при регистрации по email)
  - `smsProvider`: `String` (по умолчанию 'smsru')

- API endpoints:
  - `GET /api/settings/registration` - получение настроек
  - `PUT /api/settings/registration` - обновление настроек

- UI компонент в админке для управления настройками

### Этап 4: Рефакторинг регистрации

**Новый endpoint:** `POST /api/register`

**Поддержка настраиваемых режимов:**

1. **Режим `email_or_phone` - регистрация по Email:**
```json
{
  "name": "John Doe",
  "email": "user@example.com",
  "password": "securePassword123"
}
```

2. **Режим `email_or_phone` - регистрация по Телефону:**
```json
{
  "name": "John Doe",
  "phone": "+79991234567",
  "password": "securePassword123"
}
```

3. **Режим `email_and_phone` - регистрация по Email И Телефону:**
```json
{
  "name": "John Doe",
  "email": "user@example.com",
  "phone": "+79991234567",
  "password": "securePassword123"
}
```

**Процесс регистрации:**

1. Загрузка настроек регистрации
2. Определение режима регистрации
3. Валидация данных в зависимости от режима
4. Проверка уникальности (email и/или phone)
5. Хэширование пароля
6. Создание пользователя:
   - `isActive: false` до верификации email (если регистрация по email)
   - После верификации email: `isActive: true`, но с ограничениями до верификации телефона
7. Генерация кодов верификации:
   - Код для email (если регистрация по email)
   - Код для phone (если регистрация по phone или режим `email_and_phone`)
8. Отправка кодов (email с токеном, SMS через SMS.ru)
9. Возврат ответа с информацией о необходимости верификации

### Этап 4: Endpoints для верификации

**1. Подтверждение Email:**
```
POST /api/verify/email
{
  "token": "verification-token" // из ссылки в письме
}
```

**2. Отправка SMS кода:**
```
POST /api/verify/phone/send
{
  "phone": "+79991234567"
}
```

**3. Проверка SMS кода:**
```
POST /api/verify/phone/check
{
  "phone": "+79991234567",
  "code": "123456"
}
```

**4. Повторная отправка кода:**
```
POST /api/verify/resend
{
  "type": "email" | "phone",
  "identifier": "email@example.com" | "+79991234567"
}
```

### Этап 5: Валидация и схемы Zod

**Создать схемы валидации:**

```typescript
// src/lib/validations/registration-schemas.ts

// Схема для режима email_or_phone (email ИЛИ phone)
export const emailRegistrationSchema = z.object({
  name: fullNameSchema.optional(),
  email: emailSchema,
  password: passwordSchema
})

export const phoneRegistrationSchema = z.object({
  name: fullNameSchema.optional(),
  phone: phoneSchema, // +79991234567
  password: passwordSchema
})

// Схема для режима email_and_phone (email И phone)
export const emailAndPhoneRegistrationSchema = z.object({
  name: fullNameSchema.optional(),
  email: emailSchema,
  phone: phoneSchema,
  password: passwordSchema
})

// Динамическая схема на основе настроек регистрации
export const registrationSchema = (mode: 'email_or_phone' | 'email_and_phone') => {
  if (mode === 'email_and_phone') {
    return emailAndPhoneRegistrationSchema
  }
  return z.union([emailRegistrationSchema, phoneRegistrationSchema])
}
```

### Этап 6: Интеграция SMS.ru

**Выбранный провайдер:** SMS.ru

**Реализация:**
- HTTP клиент для API SMS.ru
- Метод `sms/send` для отправки SMS
- Обработка ответов и статусов доставки
- Rate limiting (1 SMS в минуту на номер)
- Мониторинг баланса через API
- Обработка ошибок с retry механизмом
- Логирование всех отправок

### Этап 7: Обновление UI

**Изменения в компонентах:**

1. **Register.tsx:**
   - Загрузка настроек регистрации из API
   - Отображение формы в зависимости от режима:
     - Режим `email_or_phone`: выбор между email и phone (radio buttons)
     - Режим `email_and_phone`: поля для email И phone одновременно
   - Валидация в реальном времени
   - Показ статуса верификации после регистрации
   - Редирект на страницу верификации

2. **VerifyEmail.tsx:**
   - Проверка токена из URL
   - Отправка запроса на верификацию
   - После успешной верификации:
     - Показ сообщения о доступе в админку
     - Предупреждение о необходимости верификации телефона для управления
     - Кнопка "Добавить и верифицировать телефон" (если телефон не указан)
     - Кнопка "Верифицировать телефон" (если телефон указан)
   - Повторная отправка письма
   - Редирект в админку

3. **VerifyPhone.tsx (новый):**
   - Проверка, указан ли телефон у пользователя
   - Если не указан: форма для ввода номера телефона
   - Если указан: показ номера и кнопка отправки SMS
   - Ввод SMS кода (6 цифр)
   - Проверка кода
   - Показ статуса верификации
   - Кнопка повторной отправки кода с таймером (60 секунд)
   - После успешной верификации: сообщение о полном доступе

4. **Компонент статуса верификации:**
   - Показ текущего статуса (email verified, phone verified)
   - Индикаторы верификации
   - Ссылки на страницы верификации
   - Использовать в профиле пользователя и в админке

### Этап 8: Ограничение функционала до верификации

**Система уровней доступа:**

```typescript
// src/utils/verification/verification-levels.ts

export enum VerificationLevel {
  NONE = 'NONE',           // Нет верификации
  EMAIL_ONLY = 'EMAIL_ONLY', // Верифицирован email (может видеть админку)
  PHONE_ONLY = 'PHONE_ONLY', // Верифицирован телефон (редкий случай)
  FULL = 'FULL'            // Верифицированы email и телефон (полный доступ)
}

export function getVerificationLevel(user: User): VerificationLevel {
  const isEmailVerified = user.email && user.emailVerified
  const isPhoneVerified = user.phone && user.phoneVerified
  
  if (isEmailVerified && isPhoneVerified) return VerificationLevel.FULL
  if (isEmailVerified) return VerificationLevel.EMAIL_ONLY
  if (isPhoneVerified) return VerificationLevel.PHONE_ONLY
  return VerificationLevel.NONE
}

export function requireEmailVerification(user: User) {
  if (!user.email || !user.emailVerified) {
    throw new Error('Email verification required')
  }
}

export function requirePhoneVerification(user: User) {
  if (!user.phone || !user.phoneVerified) {
    throw new Error('Phone verification required')
  }
}

export function requireFullVerification(user: User) {
  requireEmailVerification(user)
  requirePhoneVerification(user)
}
```

**Где применять:**
- **Просмотр админки**: требует `EMAIL_ONLY` или выше
- **Создание объявлений**: требует `FULL`
- **Отправка сообщений**: требует `FULL`
- **Публикация контента**: требует `FULL`
- **Управление настройками**: требует `FULL`
- **Другие критичные действия**: требует `FULL`

---

## 📋 План реализации

### Фаза 1: Подготовка схемы БД и настроек (2-3 дня)

- [ ] Обновить Prisma schema (phone, phoneVerified, email опциональный)
- [ ] Создать модель VerificationCode
- [ ] Создать систему настроек регистрации (модель или JSON файл)
- [ ] Создать миграции
- [ ] Обновить индексы
- [ ] Создать API для настроек регистрации
- [ ] Создать UI для управления настройками в админке

### Фаза 2: Валидация и схемы (1-2 дня)

- [ ] Создать схемы валидации для регистрации (с поддержкой режимов)
- [ ] Добавить валидацию телефона
- [ ] Создать схемы валидации для верификации
- [ ] Обновить существующие схемы

### Фаза 3: Рефакторинг регистрации (2-3 дня)

- [ ] Обновить `POST /api/register` для поддержки режимов
- [ ] Реализовать генерацию кодов верификации
- [ ] Интегрировать отправку email с токеном
- [ ] Обновить логику создания пользователя (isActive в зависимости от верификации)
- [ ] Добавить поддержку режима `email_and_phone`

### Фаза 4: Endpoints верификации (2-3 дня)

- [ ] `POST /api/verify/email` - подтверждение email (доступ в админку)
- [ ] `POST /api/verify/phone/send` - отправка SMS через SMS.ru
- [ ] `POST /api/verify/phone/check` - проверка SMS кода (полный доступ)
- [ ] `POST /api/verify/resend` - повторная отправка

### Фаза 5: Интеграция SMS.ru (2-3 дня)

- [ ] Изучить API SMS.ru
- [ ] Создать абстракцию SMS-сервиса
- [ ] Реализовать отправку SMS через SMS.ru
- [ ] Добавить rate limiting (1 SMS в минуту)
- [ ] Добавить мониторинг баланса
- [ ] Добавить логирование и обработку ошибок

### Фаза 6: Обновление UI (2-3 дня)

- [ ] Обновить Register.tsx для поддержки режимов
- [ ] Активировать VerifyEmail.tsx (с предупреждением о верификации телефона)
- [ ] Создать VerifyPhone.tsx
- [ ] Создать компонент статуса верификации
- [ ] Добавить проверки верификации в UI
- [ ] Добавить баннеры и ограничения

### Фаза 7: Ограничение функционала (2-3 дня)

- [ ] Создать систему уровней доступа (NONE, EMAIL_ONLY, PHONE_ONLY, FULL)
- [ ] Создать middleware для проверки верификации
- [ ] Применить к критичным endpoints (просмотр админки, управление)
- [ ] Обновить UI для показа ограничений и статуса верификации

### Фаза 8: Тестирование (2-3 дня)

- [ ] Unit тесты для валидации
- [ ] Unit тесты для SMS-сервиса (с моками)
- [ ] Integration тесты для регистрации в разных режимах
- [ ] E2E тесты для полного flow (email → админка → phone → управление)
- [ ] Тесты производительности

---

## ⚠️ Риски и митигация

### Риск 1: Миграция существующих пользователей
- **Проблема:** У существующих пользователей `email` обязателен, но нужно сделать опциональным
- **Митигация:** Поэтапная миграция, сначала добавить `phone`, затем сделать `email` опциональным

### Риск 2: Стоимость SMS через SMS.ru
- **Проблема:** SMS.ru взимает плату за каждую отправку SMS
- **Митигация:** Rate limiting (1 SMS в минуту), мониторинг баланса, уведомления при низком балансе

### Риск 3: Доступность SMS.ru API
- **Проблема:** API SMS.ru может быть недоступен или иметь ограничения
- **Митигация:** Retry механизм при временных ошибках, логирование всех ошибок, уведомление администратора, возможность ручной верификации в админке

### Риск 4: Ограничения SMS.ru
- **Проблема:** SMS.ru может иметь ограничения на количество SMS в день/месяц
- **Митигация:** Мониторинг лимитов, предупреждения при приближении к лимиту

### Риск 5: Безопасность кодов
- **Проблема:** Коды могут быть перехвачены или подобраны
- **Митигация:** Ограничение попыток, короткое время жизни кода, rate limiting

---

## 📈 Оценка времени

**Общее время:** 15-22 дня

**Разбивка:**
- Подготовка схемы и настроек: 2-3 дня
- Валидация: 1-2 дня
- Рефакторинг регистрации: 2-3 дня
- Endpoints верификации: 2-3 дня
- Интеграция SMS.ru: 2-3 дня
- UI обновления: 2-3 дня
- Ограничение функционала: 2-3 дня
- Тестирование: 2-3 дня

**Разбивка:**
- Подготовка схемы и настроек: 2-3 дня
- Валидация: 1-2 дня
- Рефакторинг регистрации: 2-3 дня
- Endpoints верификации: 2-3 дня
- Интеграция SMS.ru: 2-3 дня
- UI обновления: 2-3 дня
- Ограничение функционала: 2-3 дня
- Тестирование: 2-3 дня

---

## 🔗 Связанные документы

- [План оставшихся задач для модуля User](plans/active/plan-user-module-remaining-tasks-2025-11-24.md)
- `src/app/api/register/route.ts` - текущая реализация регистрации
- `prisma/schema.prisma` - схема базы данных

---

## ✅ Выводы

Текущая система регистрации требует значительного рефакторинга для поддержки:
1. ✅ Настраиваемых режимов регистрации (email_or_phone / email_and_phone) в админке
2. ✅ Подтверждения email (обязательное при регистрации по email)
3. ✅ Регистрации по телефону (альтернатива или дополнение к email)
4. ✅ Верификации телефона через SMS.ru (обязательная для полного доступа)
5. ✅ Системы уровней доступа (NONE, EMAIL_ONLY, PHONE_ONLY, FULL)
6. ✅ Ограничения функционала до верификации телефона

**Критичные изменения:**
- Расширение схемы User (phone, phoneVerified, email опциональный)
- Создание системы настроек регистрации в админке
- Рефакторинг процесса регистрации с поддержкой режимов
- Интеграция SMS.ru для отправки SMS
- Система уровней доступа и ограничение функционала
- Обновление UI компонентов

**Рекомендация:** Начать с Фазы 1-2 (схема, настройки, валидация), затем рефакторинг регистрации, верификация email, интеграция SMS.ru, и в конце ограничение функционала.

