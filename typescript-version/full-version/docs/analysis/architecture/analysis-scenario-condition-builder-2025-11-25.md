# Анализ: Визуальный конструктор условий для сценариев

**Дата:** 2025-11-25  
**Статус:** В работе

---

## 📋 Цель

Создать визуальный интерфейс (Condition Builder) для построения сложных логических условий в сценариях уведомлений без написания кода.

---

## 📊 Текущее состояние

### Существующая система условий

**Типы условий (`src/services/notifications/scenarios/types.ts`):**

```typescript
interface TriggerCondition {
  field: string       // Путь к полю: 'user.role', 'payload.amount'
  operator: 'eq' | 'ne' | 'gt' | 'gte' | 'lt' | 'lte' | 'in' | 'contains' | 'exists'
  value: any
}

interface ScenarioConditions {
  userRole?: string[]     // Быстрые фильтры по роли
  userStatus?: string[]   // Быстрые фильтры по статусу
  custom?: ActionCondition[]  // Кастомные условия
}
```

### Ограничения текущей системы

1. **Нет вложенности** — только плоский список условий (неявный AND)
2. **Нет OR/NOT логики** — все условия объединяются через AND
3. **Нет UI** — условия задаются только через JSON
4. **Нет валидации полей** — пользователь должен знать структуру данных

---

## 🎯 Требования к конструктору

### Функциональные требования

| # | Требование | Приоритет |
|---|------------|-----------|
| 1 | Группировка условий (AND/OR) | Высокий |
| 2 | Вложенные группы | Высокий |
| 3 | Автокомплит полей | Средний |
| 4 | Превью результата (JSON) | Средний |
| 5 | Drag-and-drop перестановка | Низкий |
| 6 | Шаблоны условий | Низкий |

### Операторы

| Оператор | Описание | Типы значений |
|----------|----------|---------------|
| `eq` | Равно | string, number, boolean |
| `ne` | Не равно | string, number, boolean |
| `gt` | Больше | number, date |
| `gte` | Больше или равно | number, date |
| `lt` | Меньше | number, date |
| `lte` | Меньше или равно | number, date |
| `in` | Входит в список | array |
| `contains` | Содержит подстроку | string |
| `exists` | Поле существует | - |
| `isEmpty` | Пустое значение | - |
| `regex` | Регулярное выражение | string |

### Доступные поля

```typescript
const AVAILABLE_FIELDS = {
  // Пользователь
  'user.id': { label: 'ID пользователя', type: 'string' },
  'user.email': { label: 'Email', type: 'string' },
  'user.role': { label: 'Роль', type: 'string', options: ['user', 'admin', 'superadmin'] },
  'user.status': { label: 'Статус', type: 'string', options: ['active', 'blocked', 'pending'] },
  'user.emailVerified': { label: 'Email подтверждён', type: 'boolean' },
  
  // Событие
  'event.type': { label: 'Тип события', type: 'string' },
  'event.source': { label: 'Источник', type: 'string' },
  'event.severity': { label: 'Важность', type: 'string' },
  
  // Payload (динамические)
  'payload.*': { label: 'Данные события', type: 'dynamic' }
}
```

---

## 🏗️ Архитектура компонента

### Структура данных

```typescript
// Новая структура условий с группами
interface ConditionGroup {
  id: string
  type: 'group'
  operator: 'and' | 'or'
  children: (ConditionRule | ConditionGroup)[]
}

interface ConditionRule {
  id: string
  type: 'rule'
  field: string
  operator: ConditionOperator
  value: any
}

type ConditionOperator = 
  | 'eq' | 'ne' 
  | 'gt' | 'gte' | 'lt' | 'lte'
  | 'in' | 'contains' | 'exists' | 'isEmpty' | 'regex'

// Корневой элемент всегда группа
type ConditionTree = ConditionGroup
```

### Компоненты UI

```
ConditionBuilder/
├── ConditionBuilder.tsx      # Главный компонент
├── ConditionGroup.tsx        # Группа условий (AND/OR)
├── ConditionRule.tsx         # Одно условие
├── FieldSelect.tsx           # Выбор поля с автокомплитом
├── OperatorSelect.tsx        # Выбор оператора
├── ValueInput.tsx            # Ввод значения (адаптивный)
├── AddButton.tsx             # Кнопка добавления
└── types.ts                  # Типы
```

### Визуальное представление

```
┌─────────────────────────────────────────────────────────┐
│ ○ AND  ○ OR                                    [+ Группа]│
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [user.role ▼] [равно ▼] [admin        ] [×]        │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [user.status▼] [не равно▼] [blocked    ] [×]       │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌───────────────────────────────────────────────────┐   │
│ │ ○ AND  ○ OR                              [×]      │   │
│ │ ┌─────────────────────────────────────────────┐   │   │
│ │ │ [payload.amount▼] [больше▼] [1000   ] [×]  │   │   │
│ │ └─────────────────────────────────────────────┘   │   │
│ │ [+ Условие]                                       │   │
│ └───────────────────────────────────────────────────┘   │
│                                                         │
│ [+ Условие]                                             │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Интеграция с ScenarioEngine

### Обновление проверки условий

```typescript
// Текущая реализация (плоский AND)
private checkConditions(conditions: ScenarioConditions, context): boolean {
  if (conditions.custom) {
    for (const condition of conditions.custom) {
      if (!this.checkCondition(condition, context)) {
        return false // AND логика
      }
    }
  }
  return true
}

// Новая реализация (рекурсивная с AND/OR)
private evaluateConditionTree(node: ConditionGroup | ConditionRule, context): boolean {
  if (node.type === 'rule') {
    return this.evaluateRule(node, context)
  }
  
  const results = node.children.map(child => 
    this.evaluateConditionTree(child, context)
  )
  
  return node.operator === 'and'
    ? results.every(r => r)
    : results.some(r => r)
}
```

---

## 📦 Зависимости

| Библиотека | Назначение | Уже есть? |
|------------|------------|-----------|
| `@dnd-kit/core` | Drag-and-drop | ❌ Нужно установить |
| `uuid` | Генерация ID | ✅ Есть |
| `MUI` | UI компоненты | ✅ Есть |

---

## 📈 Оценка трудозатрат

| Этап | Часы |
|------|------|
| Типы и структуры данных | 1 |
| ConditionBuilder компонент | 4 |
| ConditionGroup компонент | 2 |
| ConditionRule компонент | 2 |
| FieldSelect с автокомплитом | 1 |
| ValueInput (адаптивный) | 1 |
| Интеграция в форму сценария | 2 |
| Обновление ScenarioEngine | 2 |
| Тестирование | 2 |
| **Итого** | **~17 часов** |

---

## ✅ Рекомендация

Реализовать в 2 этапа:

**Этап 1 (MVP):**
- Группы AND/OR
- Базовые операторы
- Интеграция в форму

**Этап 2 (Улучшения):**
- Drag-and-drop
- Автокомплит полей
- Шаблоны условий

---

## 🔗 Связанные документы

- [План модуля сценариев](../../plans/active/plan-notification-scenarios-module-2025-01-24.md)
- [Типы сценариев](../../../src/services/notifications/scenarios/types.ts)




