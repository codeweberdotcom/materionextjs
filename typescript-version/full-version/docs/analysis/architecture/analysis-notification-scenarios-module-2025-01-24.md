# Анализ модуля сценариев уведомлений (Notification Scenarios)

**Дата проведения:** 2025-01-24  
**Статус:** В работе  
**Приоритет:** Высокий

---

## 🎯 Цель анализа

Провести комплексный анализ текущего состояния систем уведомлений и коммуникаций в проекте для разработки модуля "Сценарии" - единой системы управления автоматизированными сценариями отправки уведомлений через различные каналы (Email, SMS, браузер, Telegram и др.) на основе событий системы.

---

## 📊 Текущее состояние

### Что анализируется:

1. **Система Email уведомлений**
   - `src/utils/email.ts` - основная утилита отправки писем
   - `src/services/external/emailService.ts` - сервис для внешних интеграций
   - `src/app/api/settings/email-templates/` - API управления шаблонами
   - `src/app/api/email/send/route.ts` - API отправки писем

2. **Система SMS уведомлений**
   - Интеграция с SMS.ru (SMSRuProvider)
   - `src/app/api/settings/sms-ru/` - настройки SMS
   - Используется в регистрации для отправки кодов верификации

3. **Система браузерных уведомлений**
   - `src/lib/sockets/namespaces/notifications/index.ts` - Socket.IO namespace
   - `src/app/api/notifications/` - REST API для уведомлений
   - `src/hooks/useNotifications.ts` - React hook для работы с уведомлениями

4. **Система событий (Events)**
   - `src/services/events/EventService.ts` - централизованный журнал событий
   - EventEmitter для pub/sub механизма
   - Интеграция с различными модулями (registration, auth, rate-limit)

5. **Telegram интеграция**
   - ❌ Отсутствует в текущей реализации

### Методология:

- Анализ исходного кода всех систем уведомлений
- Изучение интеграций между модулями
- Анализ существующих триггеров и точек отправки
- Оценка архитектурных паттернов
- Изучение лучших практик в больших проектах

---

## 🔍 Результаты анализа

### 1. Текущая архитектура уведомлений

#### Email система

**Компоненты:**
- ✅ Шаблоны писем с Handlebars
- ✅ Планирование отправки (cron-based queue)
- ✅ Поддержка вложений и embedded images
- ✅ DKIM и S/MIME (в документации, но не реализовано)
- ✅ Webhook tracking (в документации)

**Проблемы:**
- ❌ Отсутствует связь с системой событий
- ❌ Хардкод отправки в разных местах кода
- ❌ Нет единой точки управления триггерами
- ⚠️ In-memory очередь (данные теряются при перезапуске)

**Примеры использования:**
```typescript
// В регистрации (src/app/api/register/route.ts:337)
await emailService.sendEmail({
  to: email,
  subject: 'Подтвердите ваш email',
  html: `...`
})

// В userService (src/services/business/userService.ts:38)
await emailService.sendEmail({
  to: user.email,
  subject: 'Welcome to our platform',
  html: `<h1>Welcome ${user.name}!</h1>...`
})
```

#### SMS система

**Компоненты:**
- ✅ Интеграция с SMS.ru
- ✅ Настройки через админку (`/apps/settings/sms-ru`)
- ✅ Отправка кодов верификации

**Проблемы:**
- ❌ Нет шаблонов для SMS
- ❌ Нет связи с системой событий
- ❌ Хардкод отправки только в регистрации
- ❌ Нет системы планирования для SMS

**Пример использования:**
```typescript
// В регистрации (src/app/api/register/route.ts:389)
const smsResult = await smsProvider.sendCode(normalizedPhone, phoneCode)
```

#### Браузерные уведомления

**Компоненты:**
- ✅ Socket.IO для real-time доставки
- ✅ REST API для CRUD операций
- ✅ Хранение в БД (Prisma Notification model)
- ✅ React hooks для клиентской работы

**Проблемы:**
- ❌ Нет связи с системой событий
- ❌ Нет автоматических триггеров
- ❌ Только ручная отправка через API

**Пример использования:**
```typescript
// sendNotificationToUser (src/lib/sockets/namespaces/notifications/index.ts:336)
export const sendNotificationToUser = async (
  userId: string,
  notificationData: { title: string; message: string; ... }
)
```

#### Система событий (Events)

**Компоненты:**
- ✅ EventService с EventEmitter
- ✅ Запись событий в БД
- ✅ Подписка на события через `onEvent()`
- ✅ Интеграция с метриками

**Проблемы:**
- ❌ Нет обработчиков событий для автоматических действий
- ❌ События только записываются, но не триггерят действия
- ❌ Нет системы правил/сценариев

**Пример записи события:**
```typescript
// В регистрации (src/app/api/register/route.ts:349)
await eventService.record({
  source: 'registration',
  type: 'verification_code_sent',
  severity: 'info',
  message: 'Email verification code sent',
  ...
})
```

### 2. Выявленные проблемы

#### Критичные проблемы:

1. **Отсутствие единой системы управления триггерами**
   - Каждый модуль отправляет уведомления самостоятельно
   - Нет централизованного управления
   - Сложно отслеживать все места отправки

2. **Нет связи между событиями и действиями**
   - События записываются, но не триггерят автоматические действия
   - Нет обработчиков событий для уведомлений
   - Нет системы правил "если событие X, то действие Y"

3. **Хардкод логики отправки**
   - Логика отправки разбросана по коду
   - Сложно изменить поведение без изменения кода
   - Нет возможности управлять через админку

4. **Отсутствие планирования и отложенных отправок**
   - Нет системы для "отправить через N дней"
   - Нет напоминаний (например, если пользователь не подтвердил email)
   - Базовая очередь только для email, но не для других каналов

#### Высокоприоритетные проблемы:

5. **Нет шаблонов для SMS и других каналов**
   - Только email имеет систему шаблонов
   - SMS отправляется с хардкод текстом
   - Нет единой системы шаблонов

6. **Отсутствие Telegram интеграции**
   - Telegram не реализован
   - Нет возможности отправлять уведомления в Telegram

7. **Нет системы сценариев (workflows)**
   - Невозможно создать сложные сценарии (например: "отправить email при регистрации, затем SMS через день если не подтвердил")
   - Нет визуального редактора сценариев
   - Нет возможности управлять через админку

8. **Отсутствие метрик и аналитики**
   - Нет отслеживания эффективности уведомлений
   - Нет статистики по каналам
   - Нет A/B тестирования

### 3. Анализ лучших практик

#### Как это делают в больших проектах:

1. **Event-Driven Architecture**
   - События триггерят обработчики
   - Обработчики могут отправлять уведомления
   - Система правил для связывания событий и действий

2. **Workflow/Scenario Engine**
   - Визуальный редактор сценариев
   - Управление через админку
   - Поддержка условий, циклов, задержек

3. **Unified Notification Service**
   - Единый сервис для всех каналов
   - Абстракция над различными провайдерами
   - Единый интерфейс для отправки

4. **Job Queue для планирования**
   - Redis/Bull для очередей
   - Планирование отложенных задач
   - Retry механизм

5. **Template Management**
   - Единая система шаблонов для всех каналов
   - Версионирование шаблонов
   - Локализация

6. **Analytics & A/B Testing**
   - Отслеживание открытий, кликов
   - A/B тестирование сообщений
   - Оптимизация конверсии

---

## 💡 Рекомендации

### 1. Создание модуля "Сценарии" (Notification Scenarios)

**Описание решения:**
Создать централизованный модуль для управления автоматизированными сценариями отправки уведомлений через различные каналы на основе событий системы.

**Архитектура:**

```
┌─────────────────┐
│  Event Service  │ ──┐
└─────────────────┘   │
                      │
┌─────────────────┐   │    ┌──────────────────────┐
│  Business Logic │ ──┼───▶│  Scenario Engine     │
└─────────────────┘   │    │  (Rule Processor)    │
                      │    └──────────────────────┘
┌─────────────────┐   │              │
│  User Actions  │ ──┘              │
└─────────────────┘                  │
                                     ▼
                          ┌──────────────────────┐
                          │  Notification Queue │
                          │  (Redis/Bull)       │
                          └──────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                ▼                ▼
            ┌──────────┐    ┌──────────┐    ┌──────────┐
            │  Email   │    │   SMS    │    │ Browser  │
            │ Provider │    │ Provider │    │ Provider │
            └──────────┘    └──────────┘    └──────────┘
                    │                │                │
                    ▼                ▼                ▼
            ┌──────────┐    ┌──────────┐    ┌──────────┐
            │ Telegram │    │  Push    │    │  Webhook │
            │ Provider │    │ Provider │    │ Provider │
            └──────────┘    └──────────┘    └──────────┘
```

**Компоненты:**

1. **Scenario Engine (Движок сценариев)**
   - Обработка правил и условий
   - Выполнение действий
   - Управление задержками и планированием

2. **Event Handlers (Обработчики событий)**
   - Подписка на события через EventService
   - Фильтрация событий по правилам
   - Триггеринг сценариев

3. **Notification Queue (Очередь уведомлений)**
   - Redis/Bull для очередей
   - Планирование отложенных отправок
   - Retry механизм

4. **Unified Notification Service (Единый сервис)**
   - Абстракция над всеми каналами
   - Единый интерфейс отправки
   - Обработка ошибок

5. **Template Service (Сервис шаблонов)**
   - Единая система шаблонов
   - Поддержка всех каналов
   - Версионирование

6. **Admin UI (Админка)**
   - Визуальный редактор сценариев
   - Управление правилами
   - Мониторинг отправок

**Оценка сложности:** Высокая  
**Оценка времени:** 3-4 недели

### 2. Интеграция с системой событий

**Описание решения:**
Подписаться на события через EventService.onEvent() и обрабатывать их через Scenario Engine.

**Пример реализации:**
```typescript
// В ScenarioService
eventService.onEvent((event) => {
  // Найти активные сценарии для этого события
  const scenarios = await findScenariosForEvent(event)
  
  // Выполнить сценарии
  for (const scenario of scenarios) {
    await executeScenario(scenario, event)
  }
})
```

**Оценка сложности:** Средняя  
**Оценка времени:** 1 неделя

### 3. Система правил и условий

**Описание решения:**
Создать систему правил для связывания событий с действиями.

**Структура правила:**
```typescript
type ScenarioRule = {
  id: string
  name: string
  trigger: {
    eventSource: string
    eventType: string
    conditions?: Array<{
      field: string
      operator: 'eq' | 'ne' | 'gt' | 'lt' | 'contains'
      value: any
    }>
  }
  actions: Array<{
    type: 'email' | 'sms' | 'browser' | 'telegram'
    templateId: string
    delay?: number // в секундах
    conditions?: Array<Condition>
  }>
  enabled: boolean
}
```

**Оценка сложности:** Средняя  
**Оценка времени:** 1-2 недели

### 4. Job Queue для планирования

**Описание решения:**
Использовать Redis + Bull для очередей задач.

**Преимущества:**
- Персистентность (данные не теряются)
- Масштабируемость
- Retry механизм
- Мониторинг

**Оценка сложности:** Средняя  
**Оценка времени:** 1 неделя

### 5. Telegram интеграция

**Описание решения:**
Добавить Telegram Bot API интеграцию.

**Компоненты:**
- Telegram Bot API клиент
- Хранение chat_id пользователей
- Отправка сообщений

**Оценка сложности:** Низкая  
**Оценка времени:** 3-5 дней

### 6. Единая система шаблонов

**Описание решения:**
Расширить систему шаблонов для всех каналов.

**Структура:**
```typescript
type NotificationTemplate = {
  id: string
  name: string
  channel: 'email' | 'sms' | 'browser' | 'telegram'
  subject?: string // для email
  content: string // Handlebars template
  variables: Array<{
    name: string
    description: string
    required: boolean
  }>
}
```

**Оценка сложности:** Средняя  
**Оценка времени:** 1 неделя

### 7. Админка для управления сценариями

**Описание решения:**
Создать UI для управления сценариями через админку.

**Функциональность:**
- Создание/редактирование сценариев
- Визуальный редактор правил
- Тестирование сценариев
- Мониторинг выполнения
- Статистика

**Оценка сложности:** Высокая  
**Оценка времени:** 2-3 недели

---

## 📝 Выводы

### Текущее состояние:

1. ✅ Есть базовые системы уведомлений (Email, SMS, Browser)
2. ✅ Есть система событий
3. ❌ Нет связи между событиями и уведомлениями
4. ❌ Нет единой системы управления
5. ❌ Нет планирования и отложенных отправок
6. ❌ Нет Telegram интеграции

### Рекомендуемый подход:

1. **Этап 1: Базовая инфраструктура** (1-2 недели)
   - Job Queue (Redis + Bull)
   - Unified Notification Service
   - Базовая интеграция с событиями

2. **Этап 2: Система правил** (1-2 недели)
   - Scenario Engine
   - Event Handlers
   - Базовая система правил

3. **Этап 3: Расширение каналов** (1 неделя)
   - Telegram интеграция
   - Единая система шаблонов

4. **Этап 4: Админка** (2-3 недели)
   - UI для управления сценариями
   - Визуальный редактор
   - Мониторинг и статистика

### Следующие шаги:

1. ✅ Создать план реализации модуля
2. ✅ Определить детальную архитектуру
3. ✅ Создать ТЗ для разработки
4. ✅ Начать с Этапа 1 (базовая инфраструктура)

---

## 🔗 Связанные документы

- [План реализации модуля сценариев уведомлений](../../plans/active/plan-notification-scenarios-module-2025-01-24.md) - План реализации на основе этого анализа
- [Анализ модуля Email Templates](../email-templates-module-analysis.md)
- [Анализ модуля Event](../architecture/analysis-event-module-2025-01-24.md)
- [AI_MODULE_STANDARDS.md](../../AI_MODULE_STANDARDS.md)
- [AI_WORKFLOW_GUIDE.md](../../AI_WORKFLOW_GUIDE.md)

---

## 📅 История изменений

- 2025-01-24: Создан первоначальный анализ модуля сценариев уведомлений

