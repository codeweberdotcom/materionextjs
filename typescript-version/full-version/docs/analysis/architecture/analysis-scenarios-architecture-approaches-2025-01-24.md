# Анализ архитектурных подходов к организации сценариев

**Дата проведения:** 2025-01-24  
**Статус:** Завершен  
**Приоритет:** Высокий

---

## 🎯 Цель анализа

Определить правильную архитектуру для сценариев: универсальный модуль сценариев или отдельные модули для каждой области (уведомления, модерация и т.д.).

---

## 📊 Текущая ситуация

### Что мы делаем сейчас:

```
src/services/notifications/
├── NotificationService.ts      # Базовый сервис уведомлений
├── NotificationQueue.ts         # Очередь задач
├── channels/                    # Каналы (Email, SMS, Browser)
└── scenarios/                   # Сценарии для уведомлений (планируется)
    ├── ScenarioEngine.ts
    ├── ScenarioService.ts
    └── EventHandlers.ts
```

**Вопрос:** Правильно ли делать сценарии как часть модуля уведомлений, если сценарии могут быть и для других задач (модерация, обработка заказов и т.д.)?

---

## 🔍 Анализ типов сценариев

### 1. Сценарии уведомлений

**Характеристики:**
- Триггер: события системы (user.registered, order.created)
- Действия: отправка уведомлений через каналы
- Условия: проверка статуса пользователя, времени и т.д.
- Задержки: отложенные отправки (через 24 часа)

**Пример:**
```
Event: user.registered
  → Condition: email not verified
  → Action: send welcome email
  → Delay: 24 hours
  → Action: send reminder email (if still not verified)
```

### 2. Сценарии модерации

**Характеристики:**
- Триггер: события (ad.created, comment.posted)
- Действия: проверка, блокировка, уведомление модератора
- Условия: проверка контента, репутации пользователя
- Задержки: автоматическая публикация после проверки

**Пример:**
```
Event: ad.created
  → Condition: user.reputation < 100
  → Action: mark as pending
  → Action: notify moderator
  → Delay: 24 hours (auto-approve if no action)
```

### 3. Сценарии обработки заказов

**Характеристики:**
- Триггер: события (order.created, payment.received)
- Действия: обновление статуса, отправка уведомлений, создание задач
- Условия: проверка оплаты, наличия товара
- Задержки: автоматическая отмена неоплаченных заказов

**Пример:**
```
Event: order.created
  → Action: reserve inventory
  → Delay: 30 minutes
  → Condition: payment not received
  → Action: cancel order
  → Action: release inventory
```

---

## 🏗️ Архитектурные подходы

### Подход 1: Универсальный Workflow Engine

**Описание:**
Один модуль сценариев для всех задач с плагинной архитектурой.

**Структура:**
```
src/services/workflows/
├── WorkflowEngine.ts           # Универсальный движок
├── WorkflowService.ts          # CRUD сценариев
├── plugins/
│   ├── NotificationPlugin.ts   # Плагин для уведомлений
│   ├── ModerationPlugin.ts     # Плагин для модерации
│   └── OrderPlugin.ts          # Плагин для заказов
└── types.ts
```

**Плюсы:**
- ✅ Единая точка управления всеми сценариями
- ✅ Переиспользование логики (условия, задержки, retry)
- ✅ Единый интерфейс админки
- ✅ Легко добавлять новые типы сценариев

**Минусы:**
- ⚠️ Сложность реализации (нужна абстракция)
- ⚠️ Риск over-engineering для простых случаев
- ⚠️ Сложность тестирования

**Когда использовать:**
- ✅ Много разных типов сценариев (5+)
- ✅ Сценарии имеют общую логику
- ✅ Нужна единая админка для управления

---

### Подход 2: Доменные модули сценариев

**Описание:**
Отдельные модули сценариев для каждой области.

**Структура:**
```
src/services/
├── notifications/
│   └── scenarios/              # Сценарии уведомлений
├── moderation/
│   └── scenarios/              # Сценарии модерации
└── orders/
    └── scenarios/              # Сценарии заказов
```

**Плюсы:**
- ✅ Простота реализации
- ✅ Изоляция логики (каждый модуль независим)
- ✅ Легко тестировать
- ✅ Нет over-engineering

**Минусы:**
- ❌ Дублирование кода (условия, задержки)
- ❌ Разные интерфейсы админки
- ❌ Сложнее поддерживать общие изменения

**Когда использовать:**
- ✅ Мало типов сценариев (2-3)
- ✅ Сценарии сильно отличаются
- ✅ Нужна быстрая реализация

---

### Подход 3: Hybrid - Базовый Engine + Доменные расширения

**Описание:** (Рекомендуемый)
Базовый модуль сценариев + доменные расширения.

**Структура:**
```
src/services/
├── workflows/                  # Базовый движок
│   ├── WorkflowEngine.ts      # Универсальная логика
│   ├── WorkflowService.ts     # CRUD
│   └── types.ts
│
├── notifications/
│   ├── NotificationService.ts
│   └── scenarios/              # Расширение для уведомлений
│       ├── NotificationScenarioEngine.ts
│       └── NotificationActions.ts
│
└── moderation/
    └── scenarios/              # Расширение для модерации
        ├── ModerationScenarioEngine.ts
        └── ModerationActions.ts
```

**Архитектура:**
```typescript
// Базовый движок
class WorkflowEngine {
  execute(scenario: WorkflowScenario, context: Context) {
    // Общая логика: условия, задержки, retry
  }
}

// Доменное расширение
class NotificationScenarioEngine extends WorkflowEngine {
  execute(scenario: NotificationScenario, event: Event) {
    // Специфичная логика для уведомлений
    const result = super.execute(scenario, { event })
    // Дополнительная обработка
  }
}
```

**Плюсы:**
- ✅ Переиспользование общей логики
- ✅ Гибкость для доменных особенностей
- ✅ Единый интерфейс + специализация
- ✅ Легко расширять

**Минусы:**
- ⚠️ Нужно правильно спроектировать абстракцию
- ⚠️ Больше файлов

**Когда использовать:**
- ✅ Среднее количество типов сценариев (3-5)
- ✅ Есть общая логика, но нужна специализация
- ✅ Планируется расширение

---

## 🏆 Рекомендация для нашего проекта

### Использовать Подход 3: Hybrid

**Причины:**

1. **У нас уже есть базовая инфраструктура:**
   - EventService (события)
   - NotificationService (уведомления)
   - NotificationQueue (очередь)

2. **Сценарии уведомлений - первый кейс:**
   - Начать с доменного модуля
   - Выделить общую логику позже

3. **Планируется расширение:**
   - Модерация
   - Обработка заказов
   - Другие сценарии

### План эволюции:

#### Этап 1: Доменный модуль (текущий)
```
src/services/notifications/scenarios/
├── ScenarioEngine.ts           # Специфичный для уведомлений
├── ScenarioService.ts
└── EventHandlers.ts
```

#### Этап 2: Выделение базового движка
```
src/services/workflows/
├── BaseWorkflowEngine.ts       # Общая логика
└── types.ts

src/services/notifications/scenarios/
└── NotificationScenarioEngine.ts  # Расширяет BaseWorkflowEngine
```

#### Этап 3: Добавление новых доменов
```
src/services/moderation/scenarios/
└── ModerationScenarioEngine.ts  # Расширяет BaseWorkflowEngine
```

---

## 📚 Примеры из больших проектов

### 1. GitHub Actions
- **Базовый движок:** Workflow Engine
- **Доменные расширения:** Actions для разных задач (CI/CD, deployments, notifications)

### 2. AWS Step Functions
- **Базовый движок:** State Machine
- **Доменные расширения:** Lambda functions для разных задач

### 3. Zapier
- **Базовый движок:** Workflow Engine
- **Доменные расширения:** Apps для разных сервисов

### 4. Temporal.io
- **Базовый движок:** Workflow Engine
- **Доменные расширения:** Activities для разных задач

---

## ✅ Выводы

1. **Текущий подход (сценарии в модуле уведомлений) - ПРАВИЛЬНЫЙ для начала** ✅
   - Начинаем с доменного модуля
   - Выделяем общую логику по мере необходимости

2. **Эволюция к Hybrid подходу:**
   - Сначала: доменный модуль сценариев уведомлений
   - Потом: выделение базового движка
   - Затем: добавление других доменов

3. **Не создавать универсальный движок сразу:**
   - Риск over-engineering
   - Сложность без понимания всех требований
   - Лучше эволюционировать от конкретного к общему

---

## 🎯 Рекомендации

1. **Сейчас:** Продолжать с доменным модулем сценариев уведомлений
2. **При добавлении модерации:** Выделить базовый движок
3. **При добавлении третьего домена:** Рефакторинг к Hybrid подходу

**Принцип:** "Start specific, evolve to generic"

---

## 🔗 Связанные документы

- [План модуля сценариев уведомлений](../plans/active/plan-notification-scenarios-module-2025-01-24.md)
- [Анализ архитектуры уведомлений](analysis-notification-architecture-approaches-2025-01-24.md)

---

**Заключение:** Текущий подход (сценарии как часть модуля уведомлений) правильный для начала. По мере добавления новых типов сценариев нужно эволюционировать к Hybrid подходу с базовым движком и доменными расширениями.







